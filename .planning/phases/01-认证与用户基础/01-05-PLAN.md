---
phase: 01-认证与用户基础
plan: 05
type: execute
wave: 3
depends_on: [01-03, 01-04]
files_modified:
  - frontend/src/views/Dashboard.vue
  - frontend/src/App.vue
autonomous: true
requirements: [AUTH-02, AUTH-03, AUTH-04]
user_setup: []
must_haves:
  truths:
    - "Dashboard shows logged-in user's name"
    - "Dashboard shows user's role and studentId"
    - "Logout button exists and clears session"
    - "Page refresh keeps user logged in (AUTH-04)"
    - "Unauthorized users cannot access Dashboard"
  artifacts:
    - path: "frontend/src/views/Dashboard.vue"
      provides: "Post-login dashboard showing user info"
      contains: "useAuthStore"
      contains: "logout"
      min_lines: 60
    - path: "frontend/src/App.vue"
      provides: "Application root with router-view"
      contains: "router-view"
      min_lines: 20
  key_links:
    - from: "frontend/src/views/Dashboard.vue"
      to: "frontend/src/stores/auth.store.ts"
      via: "useAuthStore"
      pattern: "useAuthStore\\(\\)"
    - from: "frontend/src/views/Dashboard.vue"
      to: "frontend/src/router/index.ts"
      via: "logout redirect"
      pattern: "router\\.push\\('/login'\\)"
    - from: "frontend/src/App.vue"
      to: "frontend/src/router/index.ts"
      via: "router-view"
      pattern: "<router-view"
---

<objective>
Create Dashboard page that displays user information and provides logout functionality.

Purpose: Verify authentication flow works end-to-end - user info displays (AUTH-02), logout works (AUTH-03), session persists (AUTH-04).
Output: Dashboard.vue showing user name/studentId/role with logout button, updated App.vue with router-view.
</objective>

<execution_context>
@/Users/liuxiangmiao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liuxiangmiao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-认证与用户基础/01-CONTEXT.md
@.planning/phases/01-认证与用户基础/01-RESEARCH.md
@.planning/phases/01-认证与用户基础/01-03-SUMMARY.md
@.planning/phases/01-认证与用户基础/01-04-SUMMARY.md

# Key decisions from CONTEXT.md
# D-04: User info: studentId, name, major, grade, class
# D-14: Logout clears state
# D-18: Pinia Store manages state

# Frontend from Plans 03-04
# auth.store.ts with user, isAuthenticated, logout
# router with guards redirecting unauthenticated to login
</context>

<interfaces>
<!-- Imports from Plan 03 auth store -->

From frontend/src/stores/auth.store.ts:
```typescript
export const useAuthStore = () => {
  user: Ref<User | null>;  // { studentId, name, role, major, grade, class }
  isAuthenticated: ComputedRef<boolean>;
  isAdmin: ComputedRef<boolean>;
  logout(): Promise<void>;
  fetchProfile(): Promise<boolean>;
}
```

<!-- Router from Plan 04 -->
From frontend/src/router/index.ts:
- Login route at /login
- Dashboard route at /dashboard (requiresAuth: true)
- router.push('/login') for logout redirect

<!-- User type -->
interface User {
  studentId: string;
  name: string;
  role: 'STUDENT' | 'ADMIN';
  major: string;
  grade: string;
  class: string;
}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Create App.vue with router-view</name>
  <files>
    frontend/src/App.vue
  </files>
  <read_first>
    - frontend/src/router/index.ts (router from Plan 04)
  </read_first>
  <action>
    Update frontend/src/App.vue to include router-view:
    
    ```vue
    <template>
      <router-view />
    </template>
    
    <script setup lang="ts">
    // Root component - router-view handles all page rendering
    </script>
    
    <style>
    /* Global styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    }
    </style>
    ```
    
    This is minimal - router-view handles all page rendering based on current route.
  </action>
  <acceptance_criteria>
    - frontend/src/App.vue exists
    - App.vue contains <router-view />
    - Global styles reset margin/padding
    - No placeholder content
  </acceptance_criteria>
  <verify>
    <automated>grep -q "router-view" frontend/src/App.vue && echo "PASS"</automated>
  </verify>
  <done>
    App.vue updated with router-view for SPA navigation.
  </done>
</task>

<task type="auto">
  <name>Task 1: Create Dashboard page showing user info</name>
  <files>
    frontend/src/views/Dashboard.vue
  </files>
  <read_first>
    - frontend/src/stores/auth.store.ts (user state from Plan 03)
    - frontend/src/router/index.ts (router for logout redirect)
    - .planning/phases/01-认证与用户基础/01-CONTEXT.md (D-04: user fields)
  </read_first>
  <action>
    Create frontend/src/views/Dashboard.vue:
    
    ```vue
    <template>
      <div class="dashboard-container">
        <el-container>
          <!-- Header with user info and logout -->
          <el-header class="dashboard-header">
            <div class="header-left">
              <h1>VibeCoding 教学实践平台</h1>
            </div>
            <div class="header-right">
              <span class="user-info">
                {{ user?.name }} ({{ user?.role === 'ADMIN' ? '管理员' : '学生' }})
              </span>
              <el-button type="danger" size="small" @click="handleLogout">
                退出登录
              </el-button>
            </div>
          </el-header>
          
          <!-- Main content -->
          <el-main class="dashboard-main">
            <el-card class="welcome-card">
              <template #header>
                <span>欢迎，{{ user?.name }}</span>
              </template>
              
              <el-descriptions :column="2" border>
                <el-descriptions-item label="学号">
                  {{ user?.studentId }}
                </el-descriptions-item>
                <el-descriptions-item label="姓名">
                  {{ user?.name }}
                </el-descriptions-item>
                <el-descriptions-item label="专业">
                  {{ user?.major }}
                </el-descriptions-item>
                <el-descriptions-item label="年级">
                  {{ user?.grade }}
                </el-descriptions-item>
                <el-descriptions-item label="班级">
                  {{ user?.class }}
                </el-descriptions-item>
                <el-descriptions-item label="角色">
                  {{ user?.role === 'ADMIN' ? '管理员' : '学生' }}
                </el-descriptions-item>
              </el-descriptions>
            </el-card>
            
            <!-- Placeholder for Phase 2 content -->
            <el-card class="projects-card">
              <template #header>
                <span>我的项目</span>
              </template>
              <el-empty description="暂无项目，请在选题管理中选择或提交选题">
                <el-button type="primary">去选题</el-button>
              </el-empty>
            </el-card>
          </el-main>
        </el-container>
      </div>
    </template>
    
    <script setup lang="ts">
    import { computed } from 'vue';
    import { useRouter } from 'vue-router';
    import { ElMessage } from 'element-plus';
    import { useAuthStore } from '@/stores/auth.store';
    
    const router = useRouter();
    const authStore = useAuthStore();
    
    // User info from auth store (AUTH-02)
    const user = computed(() => authStore.user);
    
    // Logout handler (AUTH-03, D-14)
    async function handleLogout() {
      await authStore.logout();
      ElMessage.success('已退出登录');
      router.push('/login');
    }
    </script>
    
    <style scoped>
    .dashboard-container {
      min-height: 100vh;
      background: #f5f7fa;
    }
    
    .dashboard-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: white;
      box-shadow: 0 1px 4px rgba(0, 0, 0, 0.1);
      padding: 0 20px;
    }
    
    .header-left h1 {
      font-size: 20px;
      color: #409eff;
    }
    
    .header-right {
      display: flex;
      align-items: center;
      gap: 15px;
    }
    
    .user-info {
      color: #666;
      font-size: 14px;
    }
    
    .dashboard-main {
      padding: 20px;
    }
    
    .welcome-card {
      margin-bottom: 20px;
    }
    
    .projects-card {
      min-height: 200px;
    }
    </style>
    ```
  </action>
  <acceptance_criteria>
    - frontend/src/views/Dashboard.vue exists
    - component imports useAuthStore from '@/stores/auth.store'
    - component imports useRouter from vue-router
    - component displays user name in header
    - component displays user role (管理员/学生)
    - component uses el-descriptions to show all user fields (D-04)
    - el-descriptions shows studentId, name, major, grade, class, role
    - component has logout button that calls authStore.logout
    - logout redirects to /login
    - logout shows success message
    - placeholder "我的项目" card exists (for Phase 2)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useAuthStore" frontend/src/views/Dashboard.vue && grep -q "authStore.logout" frontend/src/views/Dashboard.vue && grep -q "router.push('/login')" frontend/src/views/Dashboard.vue && grep -q "el-descriptions" frontend/src/views/Dashboard.vue && grep -q "user?.studentId" frontend/src/views/Dashboard.vue && grep -q "user?.name" frontend/src/views/Dashboard.vue && grep -q "user?.role" frontend/src/views/Dashboard.vue && echo "PASS"</automated>
  </verify>
  <done>
    Dashboard page created showing user info (AUTH-02) with logout functionality (AUTH-03), placeholder for projects.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router Guard → Dashboard | Protected route access (guarded) |
| Dashboard → Auth Store | State display (trusted internal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | I | User info display | accept | Only shows authenticated user's own data, no sensitive info |
| T-01-05-02 | T | Logout CSRF | accept | sameSite:'strict' cookie provides CSRF protection |

</threat_model>

<verification>
Wave 3 end-to-end verification:
1. Navigate to /dashboard without login redirects to /login
2. Login at /login succeeds and redirects to /dashboard
3. Dashboard shows user name and info
4. Refresh page stays logged in (AUTH-04)
5. Logout button clears session and redirects to /login
6. After logout, /dashboard redirects to /login
</verification>

<success_criteria>
- Dashboard displays user name, studentId, role (AUTH-02)
- Logout button works and clears session (AUTH-03)
- Page refresh maintains login state (AUTH-04)
- Protected route access enforced by guards
- End-to-end auth flow complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-认证与用户基础/01-05-SUMMARY.md`

This completes Phase 1 authentication implementation. All AUTH-01~04 requirements are now fulfilled.
</output>