---
phase: 02-选题管理与学生端
plan: 05
type: execute
wave: 5
depends_on: [02-03]
files_modified: [frontend/src/views/TopicPool.vue, frontend/src/components/TopicDetailDialog.vue, frontend/src/components/CustomTopicDialog.vue]
autonomous: true
requirements: [TOPIC-01, TOPIC-02, TOPIC-03, TOPIC-05]
user_setup: []

must_haves:
  truths:
    - "TopicPool.vue displays table with sidebar filter (D-16)"
    - "Sidebar has domain radio filter: 全部/软件工程/大数据 (TOPIC-02)"
    - "Table shows title/domain/type/description/actions columns"
    - "点击'详情'显示完整选题信息 (TOPIC-03)"
    - "点击'选择'创建项目并提示成功"
    - "点击'提交自拟选题'打开表单弹窗 (TOPIC-05)"
    - "Domain tag uses primary for SE, success for BD"
    - "Type tag uses default for SYSTEM, warning for CUSTOM"
  artifacts:
    - path: "frontend/src/views/TopicPool.vue"
      provides: "Topic pool browsing page (D-16)"
      min_lines: 150
      contains: "el-table", "el-aside", "el-radio-group"
    - path: "frontend/src/components/TopicDetailDialog.vue"
      provides: "Topic detail dialog (TOPIC-03)"
      min_lines: 80
      contains: "el-dialog", "el-descriptions"
    - path: "frontend/src/components/CustomTopicDialog.vue"
      provides: "Custom topic form (TOPIC-05)"
      min_lines: 100
      contains: "el-form", "el-input"
  key_links:
    - from: "frontend/src/views/TopicPool.vue"
      to: "frontend/src/stores/topic.store.ts"
      via: "useTopicStore"
      pattern: "topicStore.filteredTopics"
    - from: "frontend/src/views/TopicPool.vue"
      to: "frontend/src/stores/project.store.ts"
      via: "useProjectStore"
      pattern: "projectStore.createProject"
---

# Phase 2 Wave 5: TopicPool.vue Page

<objective>
Create TopicPool.vue page with table list and sidebar domain filter, implementing D-16 layout. Add TopicDetailDialog and CustomTopicDialog components for topic details and custom topic submission.

Purpose: Enable topic browsing (TOPIC-01), domain filtering (TOPIC-02), topic detail viewing (TOPIC-03), and custom topic submission (TOPIC-05).
Output: TopicPool.vue (main page), TopicDetailDialog.vue (detail modal), CustomTopicDialog.vue (form modal).
</objective>

<execution_context>
@/Users/liuxiangmiao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liuxiangmiao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-选题管理与学生端/02-CONTEXT.md
@.planning/phases/02-选题管理与学生端/02-RESEARCH.md
@.planning/phases/02-选题管理与学生端/02-UI-SPEC.md

<interfaces>
<!-- Key interfaces from existing codebase -->

From frontend/src/views/Dashboard.vue (Phase 1 pattern):
```vue
<script setup lang="ts">
import { computed, onMounted } from 'vue';
import { useRouter } from 'vue-router';
import { ElMessage } from 'element-plus';
import { useAuthStore } from '@/stores/auth.store';

const router = useRouter();
const authStore = useAuthStore();
const user = computed(() => authStore.user);
</script>

<template>
  <el-container>
    <el-header>...</el-header>
    <el-main>
      <el-card>...</el-card>
    </el-main>
  </el-container>
</template>
```

From frontend/src/stores/topic.store.ts (Wave 3):
```typescript
export const useTopicStore = defineStore('topic', () => {
  const topics = ref<Topic[]>([]);
  const loading = ref(false);
  const filteredTopics = computed(...);
  const selectedDomain = ref<Domain | null>(null);
  // Actions: fetchTopics, getTopicDetail, createCustomTopic, setDomainFilter
});
```

From frontend/src/stores/project.store.ts (Wave 4):
```typescript
export const useProjectStore = defineStore('project', () => {
  const projects = ref<Project[]>([]);
  const maxProjectsReached = computed(() => projects.value.length >= 10);
  // Actions: createProject, fetchProjects
});
```

From frontend/src/types/topic.ts:
```typescript
export type Domain = 'SE' | 'BD';
export type TopicType = 'SYSTEM' | 'CUSTOM';
export interface Topic { id, title, description, background, objectives, domain, techStack, type }
```
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: Create TopicDetailDialog component</name>
<files>frontend/src/components/TopicDetailDialog.vue</files>
<read_first>
- frontend/src/views/Dashboard.vue (Vue component pattern)
- frontend/src/stores/topic.store.ts (topic store)
- frontend/src/types/topic.ts (Topic interface)
- .planning/phases/02-选题管理与学生端/02-UI-SPEC.md (Detail dialog spec)
- .planning/phases/02-选题管理与学生端/02-CONTEXT.md (TOPIC-03)
</read_first>
<action>
Create TopicDetailDialog.vue for TOPIC-03 (topic detail display):

```vue
<template>
  <el-dialog
    v-model="visible"
    title="选题详情"
    width="600"
    @close="handleClose"
  >
    <el-descriptions :column="1" border v-if="topic">
      <el-descriptions-item label="标题">
        {{ topic.title }}
      </el-descriptions-item>
      <el-descriptions-item label="描述">
        {{ topic.description }}
      </el-descriptions-item>
      <el-descriptions-item label="背景">
        {{ topic.background || '暂无' }}
      </el-descriptions-item>
      <el-descriptions-item label="目标">
        {{ topic.objectives || '暂无' }}
      </el-descriptions-item>
      <el-descriptions-item label="领域">
        <el-tag :type="topic.domain === 'SE' ? 'primary' : 'success'">
          {{ topic.domain === 'SE' ? '软件工程' : '大数据' }}
        </el-tag>
      </el-descriptions-item>
      <el-descriptions-item label="类型">
        <el-tag :type="topic.type === 'SYSTEM' ? '' : 'warning'">
          {{ topic.type === 'SYSTEM' ? '系统预设' : '自拟选题' }}
        </el-tag>
      </el-descriptions-item>
      <el-descriptions-item label="技术栈建议">
        <div v-if="topic.techStack && topic.techStack.length > 0">
          <el-tag v-for="tech in topic.techStack" :key="tech" class="tech-tag">
            {{ tech }}
          </el-tag>
        </div>
        <span v-else>暂无建议</span>
      </el-descriptions-item>
    </el-descriptions>

    <template #footer>
      <el-button @click="handleClose">取消</el-button>
      <el-button type="primary" @click="handleSelect" :loading="loading">
        选择此选题
      </el-button>
    </template>
  </el-dialog>
</template>

<script setup lang="ts">
import { ref, watch } from 'vue';
import { ElMessage } from 'element-plus';
import { useProjectStore } from '@/stores/project.store';
import type { Topic } from '@/types/topic';

interface Props {
  visible: boolean;
  topic: Topic | null;
}

const props = defineProps<Props>();
const emit = defineEmits<{
  (e: 'update:visible', value: boolean): void;
  (e: 'selected'): void;
}>();

const projectStore = useProjectStore();
const loading = ref(false);

// Sync visible prop with v-model
const visible = ref(props.visible);
watch(() => props.visible, (val) => {
  visible.value = val;
});
watch(visible, (val) => {
  emit('update:visible', val);
});

async function handleSelect(): Promise<void> {
  if (!props.topic) return;

  // Check if max projects reached
  if (projectStore.maxProjectsReached) {
    ElMessage.warning('已达到项目上限(10个)，请删除现有项目后再创建');
    return;
  }

  loading.value = true;
  const success = await projectStore.createProject(props.topic.id);
  loading.value = false;

  if (success) {
    ElMessage.success('项目创建成功');
    emit('selected');
    handleClose();
  } else {
    ElMessage.error(projectStore.error || '创建项目失败');
  }
}

function handleClose(): void {
  visible.value = false;
}
</script>

<style scoped>
.tech-tag {
  margin-right: 8px;
  margin-bottom: 4px;
}
</style>
```

CRITICAL: el-descriptions MUST show ALL topic fields: title, description, background, objectives, domain, type, techStack. Domain tag MUST use primary (SE) or success (BD). Type tag MUST use default (SYSTEM) or warning (CUSTOM). "选择此选题" button MUST call projectStore.createProject(topic.id). Check maxProjectsReached before creating (D-08).
</action>
<acceptance_criteria>
- test -f frontend/src/components/TopicDetailDialog.vue
- grep -q "el-dialog" frontend/src/components/TopicDetailDialog.vue
- grep -q "el-descriptions" frontend/src/components/TopicDetailDialog.vue
- grep -q "title.*选题详情" frontend/src/components/TopicDetailDialog.vue
- grep -q "el-descriptions-item.*label.*标题" frontend/src/components/TopicDetailDialog.vue
- grep -q "el-descriptions-item.*label.*描述" frontend/src/components/TopicDetailDialog.vue
- grep -q "el-descriptions-item.*label.*背景" frontend/src/components/TopicDetailDialog.vue
- grep -q "el-descriptions-item.*label.*目标" frontend/src/components/TopicDetailDialog.vue
- grep -q "el-descriptions-item.*label.*领域" frontend/src/components/TopicDetailDialog.vue
- grep -q "el-descriptions-item.*label.*类型" frontend/src/components/TopicDetailDialog.vue
- grep -q "el-descriptions-item.*label.*技术栈" frontend/src/components/TopicDetailDialog.vue
- grep -q "topic.domain === 'SE'.*primary" frontend/src/components/TopicDetailDialog.vue
- grep -q "topic.domain === 'BD'.*success" frontend/src/components/TopicDetailDialog.vue
- grep -q "topic.type === 'SYSTEM'" frontend/src/components/TopicDetailDialog.vue
- grep -q "选择此选题" frontend/src/components/TopicDetailDialog.vue
- grep -q "projectStore.createProject" frontend/src/components/TopicDetailDialog.vue
- grep -q "maxProjectsReached" frontend/src/components/TopicDetailDialog.vue
- grep -q "ElMessage.success.*项目创建成功" frontend/src/components/TopicDetailDialog.vue
</acceptance_criteria>
<verify>
<automated>cd frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
</verify>
<done>TopicDetailDialog.vue created with el-dialog/el-descriptions, displays all topic fields (TOPIC-03), domain/type tags with correct colors, "选择此选题" button calling createProject, max limit check, v-model sync for visible.</done>
</task>

<task type="auto">
<name>Task 2: Create CustomTopicDialog component</name>
<files>frontend/src/components/CustomTopicDialog.vue</files>
<read_first>
- frontend/src/components/TopicDetailDialog.vue (dialog pattern from Task 1)
- frontend/src/stores/topic.store.ts (createCustomTopic action)
- frontend/src/types/topic.ts (CustomTopicInput)
- .planning/phases/02-选题管理与学生端/02-UI-SPEC.md (Custom topic form spec)
- .planning/phases/02-选题管理与学生端/02-CONTEXT.md (D-12, TOPIC-05)
</read_first>
<action>
Create CustomTopicDialog.vue for TOPIC-05 (custom topic submission):

```vue
<template>
  <el-dialog
    v-model="visible"
    title="提交自拟选题"
    width="500"
    @close="handleClose"
  >
    <el-form
      ref="formRef"
      :model="formData"
      :rules="formRules"
      label-width="80px"
    >
      <el-form-item label="标题" prop="title">
        <el-input
          v-model="formData.title"
          placeholder="请输入选题标题"
          maxlength="100"
          show-word-limit
        />
      </el-form-item>

      <el-form-item label="领域" prop="domain">
        <el-radio-group v-model="formData.domain">
          <el-radio-button value="SE">软件工程</el-radio-button>
          <el-radio-button value="BD">大数据</el-radio-button>
        </el-radio-group>
      </el-form-item>

      <el-form-item label="描述" prop="description">
        <el-input
          v-model="formData.description"
          type="textarea"
          :rows="3"
          placeholder="请描述选题内容"
          maxlength="500"
          show-word-limit
        />
      </el-form-item>

      <el-form-item label="背景" prop="background">
        <el-input
          v-model="formData.background"
          type="textarea"
          :rows="2"
          placeholder="选题背景（可选）"
        />
      </el-form-item>

      <el-form-item label="目标" prop="objectives">
        <el-input
          v-model="formData.objectives"
          type="textarea"
          :rows="2"
          placeholder="预期目标（可选）"
        />
      </el-form-item>
    </el-form>

    <template #footer>
      <el-button @click="handleClose">取消</el-button>
      <el-button type="primary" @click="handleSubmit" :loading="loading">
        提交选题
      </el-button>
    </template>
  </el-dialog>
</template>

<script setup lang="ts">
import { ref, watch, reactive } from 'vue';
import { ElMessage, type FormInstance, type FormRules } from 'element-plus';
import { useTopicStore } from '@/stores/topic.store';
import type { Domain } from '@/types/topic';

interface Props {
  visible: boolean;
}

const props = defineProps<Props>();
const emit = defineEmits<{
  (e: 'update:visible', value: boolean): void;
  (e: 'submitted'): void;
}>();

const topicStore = useTopicStore();
const formRef = ref<FormInstance>();
const loading = ref(false);

// Form data
const formData = reactive({
  title: '',
  domain: 'SE' as Domain,
  description: '',
  background: '',
  objectives: ''
});

// Validation rules (D-12: title required, description required, domain required)
const formRules: FormRules = {
  title: [
    { required: true, message: '请输入选题标题', trigger: 'blur' },
    { min: 2, max: 100, message: '标题长度需要在2-100字符之间', trigger: 'blur' }
  ],
  domain: [
    { required: true, message: '请选择领域', trigger: 'change' }
  ],
  description: [
    { required: true, message: '请输入选题描述', trigger: 'blur' },
    { min: 10, max: 500, message: '描述长度需要在10-500字符之间', trigger: 'blur' }
  ]
};

// Sync visible prop with v-model
const visible = ref(props.visible);
watch(() => props.visible, (val) => {
  visible.value = val;
});
watch(visible, (val) => {
  emit('update:visible', val);
});

async function handleSubmit(): Promise<void> {
  if (!formRef.value) return;

  const valid = await formRef.value.validate();
  if (!valid) return;

  loading.value = true;
  const success = await topicStore.createCustomTopic({
    title: formData.title,
    domain: formData.domain,
    description: formData.description,
    background: formData.background || undefined,
    objectives: formData.objectives || undefined
  });
  loading.value = false;

  if (success) {
    ElMessage.success('选题提交成功');
    emit('submitted');
    resetForm();
    handleClose();
  } else {
    ElMessage.error(topicStore.error || '提交选题失败');
  }
}

function resetForm(): void {
  formData.title = '';
  formData.domain = 'SE';
  formData.description = '';
  formData.background = '';
  formData.objectives = '';
  formRef.value?.resetFields();
}

function handleClose(): void {
  visible.value = false;
}
</script>

<style scoped>
.el-input__textarea {
  min-height: 60px;
}
</style>
```

CRITICAL: Form MUST have fields per D-12: title, domain, description, background, objectives. title and description MUST be required with length validation (2-100, 10-500). domain MUST have radio buttons for SE and BD (D-03). background and objectives MUST be optional (textarea). Submit MUST call topicStore.createCustomTopic. "提交选题" is the primary CTA per UI-SPEC.
</action>
<acceptance_criteria>
- test -f frontend/src/components/CustomTopicDialog.vue
- grep -q "el-dialog" frontend/src/components/CustomTopicDialog.vue
- grep -q "title.*提交自拟选题" frontend/src/components/CustomTopicDialog.vue
- grep -q "el-form" frontend/src/components/CustomTopicDialog.vue
- grep -q "el-form-item.*label.*标题" frontend/src/components/CustomTopicDialog.vue
- grep -q "el-form-item.*label.*领域" frontend/src/components/CustomTopicDialog.vue
- grep -q "el-radio-button.*value.*SE.*软件工程" frontend/src/components/CustomTopicDialog.vue
- grep -q "el-radio-button.*value.*BD.*大数据" frontend/src/components/CustomTopicDialog.vue
- grep -q "el-form-item.*label.*描述" frontend/src/components/CustomTopicDialog.vue
- grep -q "el-form-item.*label.*背景" frontend/src/components/CustomTopicDialog.vue
- grep -q "el-form-item.*label.*目标" frontend/src/components/CustomTopicDialog.vue
- grep -q "formRules.*title.*required.*true" frontend/src/components/CustomTopicDialog.vue
- grep -q "formRules.*domain.*required.*true" frontend/src/components/CustomTopicDialog.vue
- grep -q "formRules.*description.*required.*true" frontend/src/components/CustomTopicDialog.vue
- grep -q "min.*2.*max.*100" frontend/src/components/CustomTopicDialog.vue
- grep -q "min.*10.*max.*500" frontend/src/components/CustomTopicDialog.vue
- grep -q "topicStore.createCustomTopic" frontend/src/components/CustomTopicDialog.vue
- grep -q "提交选题" frontend/src/components/CustomTopicDialog.vue
- grep -q "ElMessage.success.*选题提交成功" frontend/src/components/CustomTopicDialog.vue
</acceptance_criteria>
<verify>
<automated>cd frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
</verify>
<done>CustomTopicDialog.vue created with el-dialog/el-form, all D-12 fields (title/domain/description/background/objectives), validation rules (required, length limits), domain radio buttons (SE/BD), submit calling createCustomTopic, v-model sync.</done>
</task>

<task type="auto">
<name>Task 3: Create TopicPool.vue main page</name>
<files>frontend/src/views/TopicPool.vue</files>
<read_first>
- frontend/src/views/Dashboard.vue (page layout pattern)
- frontend/src/components/TopicDetailDialog.vue (detail dialog from Task 1)
- frontend/src/components/CustomTopicDialog.vue (form dialog from Task 2)
- frontend/src/stores/topic.store.ts (topic store)
- frontend/src/stores/project.store.ts (project store)
- .planning/phases/02-选题管理与学生端/02-UI-SPEC.md (D-16 layout spec)
- .planning/phases/02-选题管理与学生端/02-CONTEXT.md (TOPIC-01, TOPIC-02)
</read_first>
<action>
Create TopicPool.vue implementing D-16 (table + sidebar filter):

```vue
<template>
  <div class="topic-pool-container">
    <el-container>
      <!-- Sidebar: Domain filter + Custom topic button -->
      <el-aside width="200px" class="sidebar">
        <el-card class="sidebar-card">
          <template #header>
            <span class="filter-header">领域筛选</span>
          </template>
          <el-radio-group
            v-model="topicStore.selectedDomain"
            @change="handleFilterChange"
            class="filter-group"
          >
            <el-radio-button :value="null">全部</el-radio-button>
            <el-radio-button value="SE">软件工程</el-radio-button>
            <el-radio-button value="BD">大数据</el-radio-button>
          </el-radio-group>
        </el-card>

        <el-button
          type="primary"
          class="custom-topic-btn"
          @click="showCustomDialog = true"
        >
          提交自拟选题
        </el-button>
      </el-aside>

      <!-- Main: Topic table -->
      <el-main class="main-content">
        <el-card>
          <el-table
            :data="topicStore.filteredTopics"
            v-loading="topicStore.loading"
            stripe
            border
            empty-text="暂无选题，系统选题由管理员添加，您也可以提交自拟选题"
          >
            <el-table-column prop="title" label="标题" width="200" show-overflow-tooltip />
            <el-table-column prop="domain" label="领域" width="100">
              <template #default="{ row }">
                <el-tag :type="row.domain === 'SE' ? 'primary' : 'success'">
                  {{ row.domain === 'SE' ? '软件工程' : '大数据' }}
                </el-tag>
              </template>
            </el-table-column>
            <el-table-column prop="type" label="类型" width="100">
              <template #default="{ row }">
                <el-tag :type="row.type === 'SYSTEM' ? '' : 'warning'">
                  {{ row.type === 'SYSTEM' ? '系统' : '自拟' }}
                </el-tag>
              </template>
            </el-table-column>
            <el-table-column prop="description" label="描述" show-overflow-tooltip />
            <el-table-column label="操作" width="150" fixed="right">
              <template #default="{ row }">
                <el-button size="small" @click="showDetail(row)">详情</el-button>
                <el-button
                  type="primary"
                  size="small"
                  @click="quickCreate(row)"
                  :disabled="projectStore.maxProjectsReached"
                >
                  选择
                </el-button>
              </template>
            </el-table-column>
          </el-table>
        </el-card>
      </el-main>
    </el-container>

    <!-- Detail dialog -->
    <TopicDetailDialog
      v-model:visible="showDetailDialog"
      :topic="currentTopic"
      @selected="handleProjectCreated"
    />

    <!-- Custom topic dialog -->
    <CustomTopicDialog
      v-model:visible="showCustomDialog"
      @submitted="handleCustomSubmitted"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue';
import { useRouter } from 'vue-router';
import { ElMessage } from 'element-plus';
import { useTopicStore } from '@/stores/topic.store';
import { useProjectStore } from '@/stores/project.store';
import TopicDetailDialog from '@/components/TopicDetailDialog.vue';
import CustomTopicDialog from '@/components/CustomTopicDialog.vue';
import type { Topic, Domain } from '@/types/topic';

const router = useRouter();
const topicStore = useTopicStore();
const projectStore = useProjectStore();

// Dialog visibility
const showDetailDialog = ref(false);
const showCustomDialog = ref(false);
const currentTopic = ref<Topic | null>(null);

// Fetch topics on mount (TOPIC-01)
onMounted(async () => {
  const success = await topicStore.fetchTopics();
  if (!success) {
    ElMessage.error(topicStore.error || '获取选题失败，请刷新页面重试');
  }
  // Also fetch projects to check max limit
  await projectStore.fetchProjects();
});

// TOPIC-02: Domain filter change
function handleFilterChange(domain: Domain | null): void {
  // Filter is handled by store's filteredTopics computed
}

// TOPIC-03: Show topic detail
function showDetail(topic: Topic): void {
  currentTopic.value = topic;
  showDetailDialog.value = true;
}

// TOPIC-04: Quick create project from table row
async function quickCreate(topic: Topic): Promise<void> {
  if (projectStore.maxProjectsReached) {
    ElMessage.warning('已达到项目上限(10个)，请删除现有项目后再创建');
    return;
  }

  const success = await projectStore.createProject(topic.id);
  if (success) {
    ElMessage.success('项目创建成功');
    // Optionally navigate to dashboard
    // router.push('/dashboard');
  } else {
    ElMessage.error(projectStore.error || '创建项目失败');
  }
}

// Handle project created from detail dialog
function handleProjectCreated(): void {
  // Refresh project list
  projectStore.fetchProjects();
}

// TOPIC-05: Handle custom topic submitted
function handleCustomSubmitted(): void {
  // Refresh topic list to show new custom topic
  topicStore.fetchTopics();
}
</script>

<style scoped>
.topic-pool-container {
  padding: 20px;
  background: #f5f7fa;
  min-height: 100vh;
}

.sidebar {
  margin-right: 20px;
}

.sidebar-card {
  margin-bottom: 20px;
}

.filter-header {
  font-size: 14px;
  font-weight: 600;
  color: #303133;
}

.filter-group {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.custom-topic-btn {
  width: 100%;
}

.main-content {
  padding: 0;
}
</style>
```

CRITICAL: Layout MUST follow D-16: el-aside (200px) + el-main. Sidebar MUST have "领域筛选" radio group with 全部/软件工程/大数据. Table MUST have columns: 标题/领域/类型/描述/操作. Domain tag colors: primary (SE), success (BD). Type tag colors: default (SYSTEM), warning (CUSTOM). "选择" button MUST be disabled when maxProjectsReached. Empty text MUST show UI-SPEC copy. "提交自拟选题" button MUST open CustomTopicDialog.
</action>
<acceptance_criteria>
- test -f frontend/src/views/TopicPool.vue
- grep -q "el-container" frontend/src/views/TopicPool.vue
- grep -q "el-aside.*width.*200px" frontend/src/views/TopicPool.vue
- grep -q "el-table" frontend/src/views/TopicPool.vue
- grep -q "领域筛选" frontend/src/views/TopicPool.vue
- grep -q "el-radio-button.*null.*全部" frontend/src/views/TopicPool.vue
- grep -q "el-radio-button.*SE.*软件工程" frontend/src/views/TopicPool.vue
- grep -q "el-radio-button.*BD.*大数据" frontend/src/views/TopicPool.vue
- grep -q "el-table-column.*label.*标题" frontend/src/views/TopicPool.vue
- grep -q "el-table-column.*label.*领域" frontend/src/views/TopicPool.vue
- grep -q "el-table-column.*label.*类型" frontend/src/views/TopicPool.vue
- grep -q "el-table-column.*label.*描述" frontend/src/views/TopicPool.vue
- grep -q "el-table-column.*label.*操作" frontend/src/views/TopicPool.vue
- grep -q "row.domain === 'SE'.*primary" frontend/src/views/TopicPool.vue
- grep -q "row.domain === 'BD'.*success" frontend/src/views/TopicPool.vue
- grep -q "row.type === 'SYSTEM'" frontend/src/views/TopicPool.vue
- grep -q "提交自拟选题" frontend/src/views/TopicPool.vue
- grep -q "TopicDetailDialog" frontend/src/views/TopicPool.vue
- grep -q "CustomTopicDialog" frontend/src/views/TopicPool.vue
- grep -q "topicStore.filteredTopics" frontend/src/views/TopicPool.vue
- grep -q "projectStore.maxProjectsReached" frontend/src/views/TopicPool.vue
- grep -q "projectStore.createProject" frontend/src/views/TopicPool.vue
- grep -q "topicStore.fetchTopics" frontend/src/views/TopicPool.vue
- grep -q "onMounted" frontend/src/views/TopicPool.vue
</acceptance_criteria>
<verify>
<automated>cd frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
</verify>
<done>TopicPool.vue created with D-16 layout (200px sidebar + table), domain filter radio group (TOPIC-02), table columns with correct tags, "详情"/"选择" buttons, max limit check, CustomTopicDialog integration, onMounted fetches topics/projects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → UI | Input validation via el-form rules |
| UI → Store | Store handles API calls with proper error handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Custom topic input | mitigate | el-form validation rules: required, min/max length |
| T-02-02 | Denial of Service | Max projects reached | mitigate | UI disables "选择" button, dialog shows warning |
| T-02-03 | Information Disclosure | Topic detail display | accept | Topic info is public for SYSTEM, private for CUSTOM (backend filters) |
</threat_model>

<verification>
1. TopicPool.vue displays table with sidebar filter
2. Domain filter shows 全部/软件工程/大数据 options
3. Table shows title/domain/type/description/actions columns
4. Domain and type tags use correct Element Plus colors
5. TopicDetailDialog shows all topic fields
6. CustomTopicDialog has proper form validation
7. TypeScript compiles without errors
</verification>

<success_criteria>
- [ ] TopicPool.vue has el-aside 200px + el-main layout (D-16)
- [ ] Sidebar has "领域筛选" radio group (TOPIC-02)
- [ ] Radio group has 全部/null, 软件工程/SE, 大数据/BD options
- [ ] Table has 标题/领域/类型/描述/操作 columns
- [ ] Domain tag: primary for SE, success for BD
- [ ] Type tag: default for SYSTEM, warning for CUSTOM
- [ ] "详情" button opens TopicDetailDialog
- [ ] "选择" button creates project, disabled at max limit
- [ ] "提交自拟选题" button opens CustomTopicDialog
- [ ] TopicDetailDialog shows all topic fields (TOPIC-03)
- [ ] CustomTopicDialog has form validation (D-12)
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-选题管理与学生端/02-05-SUMMARY.md`
</output>