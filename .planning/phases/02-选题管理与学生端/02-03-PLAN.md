---
phase: 02-选题管理与学生端
plan: 03
type: execute
wave: 3
depends_on: [02-01]
files_modified: [frontend/src/api/topic.api.ts, frontend/src/stores/topic.store.ts, frontend/src/types/topic.ts]
autonomous: true
requirements: [TOPIC-01, TOPIC-02, TOPIC-03, TOPIC-05]
user_setup: []

must_haves:
  truths:
    - "topic.api.ts exports fetchTopicsApi, getTopicDetailApi, createCustomTopicApi"
    - "topic.store.ts manages topics state with filteredTopics computed (TOPIC-02)"
    - "Domain filter computed returns filtered list based on selectedDomain"
    - "Custom topic creation updates topics array in store"
    - "Loading/error states handled for all operations"
  artifacts:
    - path: "frontend/src/api/topic.api.ts"
      provides: "Topic API client functions"
      exports: ["fetchTopicsApi", "getTopicDetailApi", "createCustomTopicApi"]
      min_lines: 50
    - path: "frontend/src/stores/topic.store.ts"
      provides: "Topic Pinia store"
      exports: ["useTopicStore"]
      contains: "filteredTopics"
      min_lines: 80
    - path: "frontend/src/types/topic.ts"
      provides: "Type definitions for Topic"
      exports: ["Topic", "Domain", "TopicType"]
      min_lines: 30
  key_links:
    - from: "frontend/src/stores/topic.store.ts"
      to: "frontend/src/api/topic.api.ts"
      via: "API function imports"
      pattern: "import.*from.*topic.api"
    - from: "frontend/src/api/topic.api.ts"
      to: "backend/src/routes/topics.routes.ts"
      via: "HTTP endpoint calls"
      pattern: "/api/topics"
---

# Phase 2 Wave 3: Frontend Topic Store + API Client

<objective>
Create frontend API client for topic endpoints and Pinia store for topic state management. Enable topic browsing, filtering, detail viewing, and custom topic creation.

Purpose: Provide frontend integration for TOPIC-01 (browse), TOPIC-02 (filter), TOPIC-03 (detail), TOPIC-05 (custom submission).
Output: topic.api.ts (API client), topic.store.ts (Pinia store), topic.ts (types).
</objective>

<execution_context>
@/Users/liuxiangmiao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liuxiangmiao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-选题管理与学生端/02-CONTEXT.md
@.planning/phases/02-选题管理与学生端/02-RESEARCH.md

<interfaces>
<!-- Key interfaces from existing codebase -->

From frontend/src/api/auth.api.ts (Phase 1 pattern):
```typescript
import { api } from '@/utils/request';

export interface User {
  studentId: string;
  name: string;
  role: 'STUDENT' | 'ADMIN';
}

export async function loginApi(studentId: string, password: string): Promise<LoginResponse> {
  return api.post<LoginResponse>('/api/auth/login', { studentId, password });
}
```

From frontend/src/utils/request.ts:
```typescript
export const api = {
  get: <T>(url: string) => request<T>(url),
  post: <T>(url: string, body: unknown) => request<T>(url, { method: 'POST', body }),
  delete: <T>(url: string) => request<T>(url, { method: 'DELETE' }),
};
```

From frontend/src/stores/auth.store.ts (Phase 1 Pinia pattern):
```typescript
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';

export const useAuthStore = defineStore('auth', () => {
  const user = ref<User | null>(null);
  const loading = ref(false);
  const error = ref<string | null>(null);
  
  const isAuthenticated = computed(() => user.value !== null);
  
  async function login(...): Promise<boolean> {
    loading.value = true;
    try {
      // API call, update state
      return true;
    } catch (e) {
      error.value = e instanceof Error ? e.message : 'Error';
      return false;
    } finally {
      loading.value = false;
    }
  }
  
  return { user, loading, error, isAuthenticated, login, $reset };
});
```

From backend/prisma/schema.prisma (Topic model):
```prisma
model Topic {
  title, description, background, objectives, domain, techStack, type, creatorId
}
enum Domain { SE, BD }
enum TopicType { SYSTEM, CUSTOM }
```
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: Create topic type definitions</name>
<files>frontend/src/types/topic.ts</files>
<read_first>
- frontend/src/api/auth.api.ts (Phase 1 type pattern)
- backend/prisma/schema.prisma (Topic model, Domain/TopicType enums)
- .planning/phases/02-选题管理与学生端/02-CONTEXT.md (D-01, D-03, D-05)
</read_first>
<action>
Create topic.ts with type definitions matching backend schema:

```typescript
/**
 * Type definitions for Topic (D-01, D-03, D-05)
 */

export type Domain = 'SE' | 'BD';

export type TopicType = 'SYSTEM' | 'CUSTOM';

export interface Topic {
  id: number;
  title: string;
  description: string;
  background: string;
  objectives: string;
  domain: Domain;
  techStack: string[];
  type: TopicType;
  creatorId?: number | null;
  createdAt: string;
  updatedAt: string;
}

export interface TopicsResponse {
  topics: Topic[];
}

export interface TopicDetailResponse {
  topic: Topic;
}

export interface CustomTopicInput {
  title: string;
  description: string;
  background?: string;
  objectives?: string;
  domain: Domain;
}

export interface CreateCustomTopicResponse {
  topic: Topic;
}
```

CRITICAL: Domain MUST have 'SE' and 'BD' values exactly (D-03). TopicType MUST have 'SYSTEM' and 'CUSTOM' values exactly (D-05). techStack MUST be string[] (not Json type). creatorId MUST be optional (null for SYSTEM topics).
</action>
<acceptance_criteria>
- test -f frontend/src/types/topic.ts
- grep -q "export type Domain" frontend/src/types/topic.ts
- grep -q "'SE' | 'BD'" frontend/src/types/topic.ts
- grep -q "export type TopicType" frontend/src/types/topic.ts
- grep -q "'SYSTEM' | 'CUSTOM'" frontend/src/types/topic.ts
- grep -q "export interface Topic" frontend/src/types/topic.ts
- grep -q "techStack.*string\[\]" frontend/src/types/topic.ts
- grep -q "creatorId.*number.*null" frontend/src/types/topic.ts
- grep -q "export interface TopicsResponse" frontend/src/types/topic.ts
- grep -q "export interface CustomTopicInput" frontend/src/types/topic.ts
</acceptance_criteria>
<verify>
<automated>cd frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
</verify>
<done>topic.ts created with Domain/TopicType types matching D-03/D-05, Topic interface with all D-01 fields, techStack as string[], creatorId optional, response/input interfaces for API.</done>
</task>

<task type="auto">
<name>Task 2: Create topic API client</name>
<files>frontend/src/api/topic.api.ts</files>
<read_first>
- frontend/src/api/auth.api.ts (Phase 1 API client pattern)
- frontend/src/utils/request.ts (api wrapper)
- frontend/src/types/topic.ts (types from Task 1)
- backend/src/routes/topics.routes.ts (endpoint structure)
</read_first>
<action>
Create topic.api.ts following auth.api.ts pattern:

```typescript
import { api } from '@/utils/request';
import type { Topic, TopicsResponse, TopicDetailResponse, CustomTopicInput, CreateCustomTopicResponse, Domain } from '@/types/topic';

/**
 * Fetch topics list (TOPIC-01)
 * Optional domain filter (TOPIC-02)
 * @param domain Optional domain filter ('SE' or 'BD')
 * @returns Topics array
 */
export async function fetchTopicsApi(domain?: Domain): Promise<TopicsResponse> {
  const url = domain ? `/api/topics?domain=${domain}` : '/api/topics';
  return api.get<TopicsResponse>(url);
}

/**
 * Get topic detail (TOPIC-03)
 * @param topicId Topic ID
 * @returns Topic detail
 */
export async function getTopicDetailApi(topicId: number): Promise<TopicDetailResponse> {
  return api.get<TopicDetailResponse>(`/api/topics/${topicId}`);
}

/**
 * Create custom topic (TOPIC-05)
 * @param data Custom topic input data
 * @returns Created topic
 */
export async function createCustomTopicApi(data: CustomTopicInput): Promise<CreateCustomTopicResponse> {
  return api.post<CreateCustomTopicResponse>('/api/topics/custom', data);
}
```

CRITICAL: fetchTopicsApi MUST support optional domain query param for filtering (TOPIC-02). All functions MUST use api wrapper from request.ts (credentials: 'include' for cookies). Endpoint paths MUST match backend routes: /api/topics, /api/topics/:id, /api/topics/custom.
</action>
<acceptance_criteria>
- test -f frontend/src/api/topic.api.ts
- grep -q "import.*api.*from.*request" frontend/src/api/topic.api.ts
- grep -q "import.*from.*types/topic" frontend/src/api/topic.api.ts
- grep -q "export async function fetchTopicsApi" frontend/src/api/topic.api.ts
- grep -q "/api/topics" frontend/src/api/topic.api.ts
- grep -q "domain.*Domain" frontend/src/api/topic.api.ts
- grep -q "?domain" frontend/src/api/topic.api.ts
- grep -q "export async function getTopicDetailApi" frontend/src/api/topic.api.ts
- grep -q "/api/topics/" frontend/src/api/topic.api.ts | grep -v "custom"
- grep -q "export async function createCustomTopicApi" frontend/src/api/topic.api.ts
- grep -q "/api/topics/custom" frontend/src/api/topic.api.ts
- grep -q "api.get.*TopicsResponse" frontend/src/api/topic.api.ts
- grep -q "api.post.*CreateCustomTopicResponse" frontend/src/api/topic.api.ts
</acceptance_criteria>
<verify>
<automated>cd frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
</verify>
<done>topic.api.ts created with three API functions: fetchTopicsApi (with optional domain filter), getTopicDetailApi, createCustomTopicApi. All use api wrapper, correct endpoints, proper type imports.</done>
</task>

<task type="auto">
<name>Task 3: Create topic Pinia store</name>
<files>frontend/src/stores/topic.store.ts</files>
<read_first>
- frontend/src/stores/auth.store.ts (Phase 1 Pinia pattern)
- frontend/src/api/topic.api.ts (API functions from Task 2)
- frontend/src/types/topic.ts (types)
- .planning/phases/02-选题管理与学生端/02-CONTEXT.md (TOPIC-02 domain filter)
</read_first>
<action>
Create topic.store.ts following auth.store.ts setup pattern:

```typescript
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';
import { fetchTopicsApi, getTopicDetailApi, createCustomTopicApi } from '@/api/topic.api';
import type { Topic, Domain, CustomTopicInput } from '@/types/topic';

/**
 * Topic Pinia store (TOPIC-01, TOPIC-02, TOPIC-05)
 * Manages topic list state and domain filtering
 */
export const useTopicStore = defineStore('topic', () => {
  // State
  const topics = ref<Topic[]>([]);
  const currentTopic = ref<Topic | null>(null);
  const loading = ref(false);
  const error = ref<string | null>(null);
  const selectedDomain = ref<Domain | null>(null);

  // Computed: TOPIC-02 domain filtering
  const filteredTopics = computed(() => {
    if (!selectedDomain.value) {
      return topics.value;
    }
    return topics.value.filter(t => t.domain === selectedDomain.value);
  });

  // Computed: Separate system and custom topics
  const systemTopics = computed(() =>
    topics.value.filter(t => t.type === 'SYSTEM')
  );

  const customTopics = computed(() =>
    topics.value.filter(t => t.type === 'CUSTOM')
  );

  // Computed: Topic count
  const topicCount = computed(() => topics.value.length);

  /**
   * Fetch topics list (TOPIC-01)
   * @returns true on success, false on failure
   */
  async function fetchTopics(): Promise<boolean> {
    loading.value = true;
    error.value = null;

    try {
      const response = await fetchTopicsApi();
      topics.value = response.topics;
      return true;
    } catch (e) {
      error.value = e instanceof Error ? e.message : '获取选题失败，请刷新页面重试';
      return false;
    } finally {
      loading.value = false;
    }
  }

  /**
   * Get topic detail (TOPIC-03)
   * @param topicId Topic ID
   * @returns true on success, false on failure
   */
  async function getTopicDetail(topicId: number): Promise<boolean> {
    loading.value = true;
    error.value = null;

    try {
      const response = await getTopicDetailApi(topicId);
      currentTopic.value = response.topic;
      return true;
    } catch (e) {
      error.value = e instanceof Error ? e.message : '获取选题详情失败';
      return false;
    } finally {
      loading.value = false;
    }
  }

  /**
   * Create custom topic (TOPIC-05)
   * @param data Custom topic input
   * @returns true on success, false on failure
   */
  async function createCustomTopic(data: CustomTopicInput): Promise<boolean> {
    loading.value = true;
    error.value = null;

    try {
      const response = await createCustomTopicApi(data);
      topics.value.push(response.topic);
      return true;
    } catch (e) {
      error.value = e instanceof Error ? e.message : '提交选题失败';
      return false;
    } finally {
      loading.value = false;
    }
  }

  /**
   * Set domain filter (TOPIC-02)
   * @param domain Domain to filter ('SE', 'BD', or null for all)
   */
  function setDomainFilter(domain: Domain | null): void {
    selectedDomain.value = domain;
  }

  /**
   * Clear current topic detail
   */
  function clearCurrentTopic(): void {
    currentTopic.value = null;
  }

  /**
   * Reset store state
   */
  function $reset(): void {
    topics.value = [];
    currentTopic.value = null;
    loading.value = false;
    error.value = null;
    selectedDomain.value = null;
  }

  return {
    // State
    topics,
    currentTopic,
    loading,
    error,
    selectedDomain,
    // Computed
    filteredTopics,
    systemTopics,
    customTopics,
    topicCount,
    // Actions
    fetchTopics,
    getTopicDetail,
    createCustomTopic,
    setDomainFilter,
    clearCurrentTopic,
    $reset
  };
});
```

CRITICAL: filteredTopics computed MUST filter by selectedDomain.value (TOPIC-02). selectedDomain MUST be Domain | null type (null shows all). createCustomTopic MUST push new topic to topics.value array. All async actions MUST set loading/error states and return boolean.
</action>
<acceptance_criteria>
- test -f frontend/src/stores/topic.store.ts
- grep -q "import.*defineStore.*pinia" frontend/src/stores/topic.store.ts
- grep -q "import.*ref.*computed.*vue" frontend/src/stores/topic.store.ts
- grep -q "import.*fetchTopicsApi.*topic.api" frontend/src/stores/topic.store.ts
- grep -q "useTopicStore.*defineStore.*topic" frontend/src/stores/topic.store.ts
- grep -q "topics.*ref.*Topic\[\]" frontend/src/stores/topic.store.ts
- grep -q "selectedDomain.*ref.*Domain.*null" frontend/src/stores/topic.store.ts
- grep -q "filteredTopics.*computed" frontend/src/stores/topic.store.ts
- grep -q "t.domain === selectedDomain.value" frontend/src/stores/topic.store.ts
- grep -q "async function fetchTopics" frontend/src/stores/topic.store.ts
- grep -q "async function createCustomTopic" frontend/src/stores/topic.store.ts
- grep -q "function setDomainFilter" frontend/src/stores/topic.store.ts
- grep -q "topics.value.push" frontend/src/stores/topic.store.ts
- grep -q "$reset" frontend/src/stores/topic.store.ts
- grep -q "return.*filteredTopics.*fetchTopics" frontend/src/stores/topic.store.ts
</acceptance_criteria>
<verify>
<automated>cd frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
</verify>
<done>topic.store.ts created with setup pattern, topics/selectedDomain state, filteredTopics computed (TOPIC-02 domain filter), fetchTopics/getTopicDetail/createCustomTopic actions, loading/error handling, $reset function, TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API Client | No threat - same frontend context |
| API Client → Backend | HTTP credentials handled by api wrapper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Domain filter value | accept | Domain is non-sensitive metadata, public info |
| T-02-02 | Tampering | Custom topic input | accept | Backend validates input, frontend uses types |

**Note:** Frontend store has no direct threat vectors - security enforced at backend route level.
</threat_model>

<verification>
1. topic.ts has correct type definitions
2. topic.api.ts has three API functions matching backend endpoints
3. topic.store.ts has filteredTopics computed for domain filtering
4. topic.store.ts has proper loading/error state handling
5. TypeScript compiles without errors
</verification>

<success_criteria>
- [ ] topic.ts has Domain and TopicType types matching D-03/D-05
- [ ] topic.ts has Topic interface with all D-01 fields
- [ ] topic.api.ts exports fetchTopicsApi, getTopicDetailApi, createCustomTopicApi
- [ ] fetchTopicsApi supports optional domain filter (TOPIC-02)
- [ ] topic.store.ts has filteredTopics computed (TOPIC-02)
- [ ] topic.store.ts has selectedDomain state for filter
- [ ] topic.store.ts has fetchTopics, getTopicDetail, createCustomTopic actions
- [ ] topic.store.ts has loading/error state handling
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-选题管理与学生端/02-03-SUMMARY.md`
</output>