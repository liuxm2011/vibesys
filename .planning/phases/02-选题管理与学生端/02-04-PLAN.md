---
phase: 02-选题管理与学生端
plan: 04
type: execute
wave: 4
depends_on: [02-02]
files_modified: [frontend/src/api/project.api.ts, frontend/src/stores/project.store.ts, frontend/src/types/project.ts]
autonomous: true
requirements: [TOPIC-04, DASH-01, DASH-02]
user_setup: []

must_haves:
  truths:
    - "project.api.ts exports fetchProjectsApi, createProjectApi, deleteProjectApi"
    - "project.store.ts manages projects state with projectCount computed (DASH-01)"
    - "Project creation updates projects array and increments count"
    - "Project deletion removes from array without affecting topics (D-09)"
    - "Project status visible in store state (DASH-02)"
    - "Max project limit (10) enforced at backend, reflected in frontend UI"
  artifacts:
    - path: "frontend/src/api/project.api.ts"
      provides: "Project API client functions"
      exports: ["fetchProjectsApi", "createProjectApi", "deleteProjectApi"]
      min_lines: 50
    - path: "frontend/src/stores/project.store.ts"
      provides: "Project Pinia store"
      exports: ["useProjectStore"]
      contains: "projects", "projectCount"
      min_lines: 80
    - path: "frontend/src/types/project.ts"
      provides: "Type definitions for Project"
      exports: ["Project", "ProjectStatus"]
      min_lines: 40
  key_links:
    - from: "frontend/src/stores/project.store.ts"
      to: "frontend/src/api/project.api.ts"
      via: "API function imports"
      pattern: "import.*from.*project.api"
    - from: "frontend/src/api/project.api.ts"
      to: "backend/src/routes/projects.routes.ts"
      via: "HTTP endpoint calls"
      pattern: "/api/projects"
---

# Phase 2 Wave 4: Frontend Project Store + API Client

<objective>
Create frontend API client for project endpoints and Pinia store for project state management. Enable project creation, listing, and deletion.

Purpose: Provide frontend integration for TOPIC-04 (project creation), DASH-01 (project list), DASH-02 (project status), D-09 (project deletion).
Output: project.api.ts (API client), project.store.ts (Pinia store), project.ts (types).
</objective>

<execution_context>
@/Users/liuxiangmiao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liuxiangmiao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-选题管理与学生端/02-CONTEXT.md
@.planning/phases/02-选题管理与学生端/02-RESEARCH.md

<interfaces>
<!-- Key interfaces from existing codebase -->

From frontend/src/api/auth.api.ts (Phase 1 pattern):
```typescript
import { api } from '@/utils/request';
export async function loginApi(...): Promise<LoginResponse> {
  return api.post<LoginResponse>('/api/auth/login', { ... });
}
```

From frontend/src/stores/auth.store.ts (Phase 1 Pinia pattern):
```typescript
export const useAuthStore = defineStore('auth', () => {
  const user = ref<User | null>(null);
  const loading = ref(false);
  const error = ref<string | null>(null);
  // Actions with try/catch/finally
  return { user, loading, error, ... };
});
```

From frontend/src/types/topic.ts (Wave 3):
```typescript
export type Domain = 'SE' | 'BD';
export type TopicType = 'SYSTEM' | 'CUSTOM';
export interface Topic { id, title, description, domain, techStack, type, ... }
```

From backend/prisma/schema.prisma (Project model):
```prisma
model Project {
  id, userId, topicId, status, documentsRef, createdAt, updatedAt
}
enum ProjectStatus { NOT_STARTED, IN_PROGRESS, COMPLETED }
```

From backend/src/routes/projects.routes.ts (Wave 2):
```typescript
// POST /api/projects - returns { project: { ...topic, ...project } }
// GET /api/projects - returns { projects: [{ topic: {...}, ...project }] }
// DELETE /api/projects/:id - returns { message: '项目已删除' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: Create project type definitions</name>
<files>frontend/src/types/project.ts</files>
<read_first>
- frontend/src/types/topic.ts (Wave 3 types)
- backend/prisma/schema.prisma (Project model, ProjectStatus enum)
- backend/src/routes/projects.routes.ts (response structure with include)
- .planning/phases/02-选题管理与学生端/02-CONTEXT.md (D-10, D-11)
</read_first>
<action>
Create project.ts with type definitions matching backend schema and response structure:

```typescript
/**
 * Type definitions for Project (D-10, D-11)
 */

import type { Domain, TopicType } from './topic';

export type ProjectStatus = 'NOT_STARTED' | 'IN_PROGRESS' | 'COMPLETED';

/**
 * Minimal topic info included in project response
 * From backend include: { select: { id, title, domain, type, techStack } }
 */
export interface ProjectTopic {
  id: number;
  title: string;
  domain: Domain;
  type: TopicType;
  techStack: string[];
}

export interface Project {
  id: number;
  userId: number;
  topicId: number;
  status: ProjectStatus;
  documentsRef: Record<string, any> | null;
  createdAt: string;
  updatedAt: string;
  topic: ProjectTopic;
}

export interface ProjectsResponse {
  projects: Project[];
}

export interface CreateProjectInput {
  topicId: number;
}

export interface CreateProjectResponse {
  project: Project;
}

export interface DeleteProjectResponse {
  message: string;
}
```

CRITICAL: ProjectStatus MUST have three values exactly matching D-10: NOT_STARTED, IN_PROGRESS, COMPLETED. Project MUST include topic field (backend uses include). documentsRef MUST be Record<string, any> | null (JSON placeholder for Phase 3).
</action>
<acceptance_criteria>
- test -f frontend/src/types/project.ts
- grep -q "export type ProjectStatus" frontend/src/types/project.ts
- grep -q "'NOT_STARTED' | 'IN_PROGRESS' | 'COMPLETED'" frontend/src/types/project.ts
- grep -q "import.*Domain.*TopicType.*topic" frontend/src/types/project.ts
- grep -q "export interface ProjectTopic" frontend/src/types/project.ts
- grep -q "export interface Project" frontend/src/types/project.ts
- grep -q "status.*ProjectStatus" frontend/src/types/project.ts
- grep -q "documentsRef.*Record" frontend/src/types/project.ts
- grep -q "topic.*ProjectTopic" frontend/src/types/project.ts
- grep -q "export interface ProjectsResponse" frontend/src/types/project.ts
- grep -q "export interface CreateProjectInput" frontend/src/types/project.ts
- grep -q "topicId.*number" frontend/src/types/project.ts
</acceptance_criteria>
<verify>
<automated>cd frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
</verify>
<done>project.ts created with ProjectStatus matching D-10, Project interface with topic field (backend include), documentsRef as Record, ProjectTopic for minimal topic info, input/response interfaces.</done>
</task>

<task type="auto">
<name>Task 2: Create project API client</name>
<files>frontend/src/api/project.api.ts</files>
<read_first>
- frontend/src/api/auth.api.ts (Phase 1 API pattern)
- frontend/src/api/topic.api.ts (Wave 3 API pattern)
- frontend/src/utils/request.ts (api wrapper)
- frontend/src/types/project.ts (types from Task 1)
- backend/src/routes/projects.routes.ts (endpoint structure)
</read_first>
<action>
Create project.api.ts following established patterns:

```typescript
import { api } from '@/utils/request';
import type { ProjectsResponse, CreateProjectInput, CreateProjectResponse, DeleteProjectResponse } from '@/types/project';

/**
 * Fetch user's projects list (DASH-01)
 * @returns Projects array with topic info
 */
export async function fetchProjectsApi(): Promise<ProjectsResponse> {
  return api.get<ProjectsResponse>('/api/projects');
}

/**
 * Create project from topic (TOPIC-04, D-06)
 * @param topicId Topic ID to create project from
 * @returns Created project with topic info
 */
export async function createProjectApi(topicId: number): Promise<CreateProjectResponse> {
  return api.post<CreateProjectResponse>('/api/projects', { topicId });
}

/**
 * Delete project (D-09)
 * @param projectId Project ID to delete
 * @returns Success message
 */
export async function deleteProjectApi(projectId: number): Promise<DeleteProjectResponse> {
  return api.delete<DeleteProjectResponse>(`/api/projects/${projectId}`);
}
```

CRITICAL: Endpoint paths MUST match backend routes: /api/projects, /api/projects/:id. createProjectApi MUST send { topicId } in body. deleteProjectApi MUST use projectId in URL path. All use api wrapper for credentials handling.
</action>
<acceptance_criteria>
- test -f frontend/src/api/project.api.ts
- grep -q "import.*api.*from.*request" frontend/src/api/project.api.ts
- grep -q "import.*from.*types/project" frontend/src/api/project.api.ts
- grep -q "export async function fetchProjectsApi" frontend/src/api/project.api.ts
- grep -q "/api/projects" frontend/src/api/project.api.ts | head -1
- grep -q "export async function createProjectApi" frontend/src/api/project.api.ts
- grep -q "topicId.*number" frontend/src/api/project.api.ts
- grep -q "api.post.*CreateProjectResponse" frontend/src/api/project.api.ts
- grep -q "export async function deleteProjectApi" frontend/src/api/project.api.ts
- grep -q "/api/projects/" frontend/src/api/project.api.ts
- grep -q "api.delete" frontend/src/api/project.api.ts
- grep -q "ProjectsResponse" frontend/src/api/project.api.ts
</acceptance_criteria>
<verify>
<automated>cd frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
</verify>
<done>project.api.ts created with three API functions: fetchProjectsApi, createProjectApi(topicId), deleteProjectApi(projectId). All use api wrapper, correct endpoints matching backend routes.</done>
</task>

<task type="auto">
<name>Task 3: Create project Pinia store</name>
<files>frontend/src/stores/project.store.ts</files>
<read_first>
- frontend/src/stores/auth.store.ts (Phase 1 Pinia pattern)
- frontend/src/stores/topic.store.ts (Wave 3 pattern)
- frontend/src/api/project.api.ts (API functions from Task 2)
- frontend/src/types/project.ts (types)
- .planning/phases/02-选题管理与学生端/02-CONTEXT.md (D-08, DASH-01, DASH-02)
</read_first>
<action>
Create project.store.ts following established Pinia setup pattern:

```typescript
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';
import { fetchProjectsApi, createProjectApi, deleteProjectApi } from '@/api/project.api';
import type { Project, ProjectStatus } from '@/types/project';

/**
 * Project Pinia store (TOPIC-04, DASH-01, DASH-02, D-08, D-09)
 * Manages project list state and project operations
 */
export const useProjectStore = defineStore('project', () => {
  // State
  const projects = ref<Project[]>([]);
  const loading = ref(false);
  const error = ref<string | null>(null);

  // Computed: DASH-01 project count
  const projectCount = computed(() => projects.value.length);

  // Computed: D-08 max limit reached check
  const maxProjectsReached = computed(() => projects.value.length >= 10);

  // Computed: DASH-02 filter by status
  const notStartedProjects = computed(() =>
    projects.value.filter(p => p.status === 'NOT_STARTED')
  );

  const inProgressProjects = computed(() =>
    projects.value.filter(p => p.status === 'IN_PROGRESS')
  );

  const completedProjects = computed(() =>
    projects.value.filter(p => p.status === 'COMPLETED')
  );

  /**
   * Fetch user's projects (DASH-01)
   * @returns true on success, false on failure
   */
  async function fetchProjects(): Promise<boolean> {
    loading.value = true;
    error.value = null;

    try {
      const response = await fetchProjectsApi();
      projects.value = response.projects;
      return true;
    } catch (e) {
      error.value = e instanceof Error ? e.message : '获取项目列表失败';
      return false;
    } finally {
      loading.value = false;
    }
  }

  /**
   * Create project from topic (TOPIC-04, D-06)
   * @param topicId Topic ID to create project from
   * @returns true on success, false on failure
   */
  async function createProject(topicId: number): Promise<boolean> {
    loading.value = true;
    error.value = null;

    try {
      const response = await createProjectApi(topicId);
      projects.value.unshift(response.project);  // Add to front (newest first)
      return true;
    } catch (e) {
      // Handle specific error messages from backend
      error.value = e instanceof Error ? e.message : '创建项目失败';
      return false;
    } finally {
      loading.value = false;
    }
  }

  /**
   * Delete project (D-09)
   * Note: Deleting project does NOT affect original topic
   * @param projectId Project ID to delete
   * @returns true on success, false on failure
   */
  async function deleteProject(projectId: number): Promise<boolean> {
    loading.value = true;
    error.value = null;

    try {
      await deleteProjectApi(projectId);
      // Remove from local state
      const index = projects.value.findIndex(p => p.id === projectId);
      if (index !== -1) {
        projects.value.splice(index, 1);
      }
      return true;
    } catch (e) {
      error.value = e instanceof Error ? e.message : '删除项目失败';
      return false;
    } finally {
      loading.value = false;
    }
  }

  /**
   * Get status display text (DASH-02)
   * @param status Project status
   * @returns Chinese display text
   */
  function getStatusText(status: ProjectStatus): string {
    switch (status) {
      case 'NOT_STARTED':
        return '未开始';
      case 'IN_PROGRESS':
        return '进行中';
      case 'COMPLETED':
        return '已完成';
    }
  }

  /**
   * Get status tag type for Element Plus (DASH-02)
   * @param status Project status
   * @returns el-tag type value
   */
  function getStatusTagType(status: ProjectStatus): 'info' | 'warning' | 'success' {
    switch (status) {
      case 'NOT_STARTED':
        return 'info';
      case 'IN_PROGRESS':
        return 'warning';
      case 'COMPLETED':
        return 'success';
    }
  }

  /**
   * Reset store state
   */
  function $reset(): void {
    projects.value = [];
    loading.value = false;
    error.value = null;
  }

  return {
    // State
    projects,
    loading,
    error,
    // Computed
    projectCount,
    maxProjectsReached,
    notStartedProjects,
    inProgressProjects,
    completedProjects,
    // Actions
    fetchProjects,
    createProject,
    deleteProject,
    getStatusText,
    getStatusTagType,
    $reset
  };
});
```

CRITICAL: projectCount MUST count projects.value.length (DASH-01). maxProjectsReached MUST check >= 10 (D-08). createProject MUST unshift to front (newest first). deleteProject MUST splice from array (D-09 - local removal). getStatusText/getStatusTagType MUST return correct Chinese text and Element Plus tag types (DASH-02).
</action>
<acceptance_criteria>
- test -f frontend/src/stores/project.store.ts
- grep -q "import.*defineStore.*pinia" frontend/src/stores/project.store.ts
- grep -q "import.*fetchProjectsApi.*project.api" frontend/src/stores/project.store.ts
- grep -q "useProjectStore.*defineStore.*project" frontend/src/stores/project.store.ts
- grep -q "projects.*ref.*Project\[\]" frontend/src/stores/project.store.ts
- grep -q "projectCount.*computed" frontend/src/stores/project.store.ts
- grep -q "projects.value.length" frontend/src/stores/project.store.ts
- grep -q "maxProjectsReached.*computed" frontend/src/stores/project.store.ts
- grep -q ">=.*10" frontend/src/stores/project.store.ts
- grep -q "async function fetchProjects" frontend/src/stores/project.store.ts
- grep -q "async function createProject" frontend/src/stores/project.store.ts
- grep -q "unshift.*response.project" frontend/src/stores/project.store.ts
- grep -q "async function deleteProject" frontend/src/stores/project.store.ts
- grep -q "splice" frontend/src/stores/project.store.ts
- grep -q "getStatusText" frontend/src/stores/project.store.ts
- grep -q "'未开始'" frontend/src/stores/project.store.ts
- grep -q "'进行中'" frontend/src/stores/project.store.ts
- grep -q "'已完成'" frontend/src/stores/project.store.ts
- grep -q "getStatusTagType" frontend/src/stores/project.store.ts
- grep -q "'info'" frontend/src/stores/project.store.ts
- grep -q "'warning'" frontend/src/stores/project.store.ts
- grep -q "'success'" frontend/src/stores/project.store.ts
- grep -q "$reset" frontend/src/stores/project.store.ts
</acceptance_criteria>
<verify>
<automated>cd frontend && npx tsc --noEmit && echo "TypeScript OK"</automated>
</verify>
<done>project.store.ts created with setup pattern, projects state, projectCount/maxProjectsReached computed (DASH-01, D-08), fetchProjects/createProject/deleteProject actions, status display helpers (DASH-02), loading/error handling, $reset, TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API Client | No threat - same frontend context |
| API Client → Backend | HTTP credentials handled by api wrapper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Project list | accept | Only user's own projects returned (backend filters by userId) |
| T-02-02 | Tampering | Project creation | accept | Backend validates limit and ownership |

**Note:** Frontend store has no direct threat vectors - security enforced at backend route level (IDOR prevention).
</threat_model>

<verification>
1. project.ts has correct type definitions
2. project.api.ts has three API functions matching backend endpoints
3. project.store.ts has projectCount computed (DASH-01)
4. project.store.ts has maxProjectsReached computed (D-08)
5. project.store.ts has status display helpers (DASH-02)
6. TypeScript compiles without errors
</verification>

<success_criteria>
- [ ] project.ts has ProjectStatus matching D-10
- [ ] project.ts has Project interface with topic field
- [ ] project.api.ts exports fetchProjectsApi, createProjectApi, deleteProjectApi
- [ ] project.store.ts has projectCount computed (DASH-01)
- [ ] project.store.ts has maxProjectsReached computed (D-08)
- [ ] project.store.ts has notStarted/inProgress/completedProjects computed (DASH-02)
- [ ] project.store.ts has getStatusText returning Chinese text (DASH-02)
- [ ] project.store.ts has getStatusTagType returning el-tag types (DASH-02)
- [ ] project.store.ts has createProject action (TOPIC-04)
- [ ] project.store.ts has deleteProject action (D-09)
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-选题管理与学生端/02-04-SUMMARY.md`
</output>