---
phase: 03-文档生成与AI服务
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - frontend/src/views/ProjectDetail.vue
  - frontend/src/router/index.ts
  - frontend/src/views/Dashboard.vue
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
  - DOC-06
  - DOC-07
user_setup: []

must_haves:
  truths:
    - "User can navigate to project detail page from dashboard"
    - "Project detail page shows document tabs with editors"
    - "User can generate documents via AI button"
    - "User can edit documents with auto-save"
    - "User can view and modify tech stack"
    - "Document changes persist across page refresh"
  artifacts:
    - path: "frontend/src/views/ProjectDetail.vue"
      provides: "Complete project detail page"
      contains: "DocumentTabs"
      contains: "MarkdownEditor"
      contains: "TechStackPanel"
      contains: "generateDocument"
    - path: "frontend/src/router/index.ts"
      provides: "Route to project detail"
      contains: "/projects/:id"
      contains: "ProjectDetail"
    - path: "frontend/src/views/Dashboard.vue"
      provides: "Navigation to project detail"
      contains: "router.push('/projects/'"
  key_links:
    - from: "ProjectDetail.vue"
      to: "document.store.ts"
      via: "useDocumentStore"
    - from: "ProjectDetail.vue"
      to: "ai.api.ts"
      via: "generateDocumentApi"
    - from: "Dashboard.vue"
      to: "/projects/:id"
      via: "handleProjectClick"
---

<objective>
Wave 4 Frontend Page Integration: Create ProjectDetail.vue page that integrates MarkdownEditor, TechStackPanel, and DocumentTabs components. Add route for /projects/:id. Update Dashboard handleProjectClick to navigate to project detail. Implement document generation buttons with loading state.

Purpose: Complete the project detail page experience connecting all components.
Output: ProjectDetail.vue, router extension, Dashboard navigation update.
</objective>

<execution_context>
@/Users/liuxiangmiao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liuxiangmiao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-文档生成与AI服务/03-CONTEXT.md
@.planning/phases/03-文档生成与AI服务/03-RESEARCH.md
</context>

<interfaces>
<!-- Key interfaces executor needs -->
From frontend/src/components/MarkdownEditor.vue (Wave 3):
```vue
<!-- Props: content, documentId, disabled -->
<!-- Emits: save, change -->
```

From frontend/src/components/TechStackPanel.vue (Wave 3):
```vue
<!-- Props: techStack, editable -->
<!-- Emits: update:techStack -->
```

From frontend/src/components/DocumentTabs.vue (Wave 3):
```vue
<!-- Props: documents, activeDocType -->
<!-- Emits: update:activeDocType -->
<!-- Slots: default for each tab content -->
```

From frontend/src/stores/document.store.ts (Wave 2):
```typescript
export const useDocumentStore = defineStore('document', () => {
  const documents = ref<Document[]>([]);
  const techStack = ref<string[]>([]);
  const generating = ref<boolean>(false);
  const loading = ref<boolean>(false);
  const error = ref<string | null>(null);
  
  // Computed
  const prdDocument = computed(...);
  const frontendDocument = computed(...);
  const backendDocument = computed(...);
  
  // Actions
  async function fetchDocuments(projectId): Promise<boolean>;
  async function updateDocument(id, content): Promise<boolean>;
  async function generateDocument(projectId, docType): Promise<boolean>;
});
```

From frontend/src/router/index.ts (existing):
```typescript
const routes: RouteRecordRaw[] = [
  { path: '/dashboard', name: 'Dashboard', component: Dashboard, meta: { requiresAuth: true } },
  { path: '/topics', name: 'TopicPool', component: TopicPool, meta: { requiresAuth: true } },
  // ...
];
```

From frontend/src/views/Dashboard.vue (existing placeholder):
```typescript
function handleProjectClick(project: Project): void {
  // TODO: Phase 3 will implement project detail page
  ElMessage.info('项目详情页面将在Phase 3实现');
}
```

From frontend/src/types/project.ts (existing):
```typescript
export interface Project {
  id: number;
  topic?: Topic;
  status: ProjectStatus;
  // ...
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ProjectDetail page layout</name>
  <files>frontend/src/views/ProjectDetail.vue</files>
  <read_first>
    - frontend/src/views/Dashboard.vue (page pattern)
    - frontend/src/views/TopicPool.vue (page pattern reference)
    - frontend/src/components/DocumentTabs.vue (tab component)
    - frontend/src/components/TechStackPanel.vue (tech stack panel)
    - frontend/src/components/MarkdownEditor.vue (editor component)
    - frontend/src/stores/document.store.ts
    - frontend/src/stores/project.store.ts
    - frontend/src/types/document.ts
    - frontend/src/types/project.ts
  </read_first>
  <action>Create ProjectDetail.vue page with complete layout. Script setup: Import all three components, useDocumentStore, useProjectStore, useRoute, useRouter. Get projectId from route.params.id. State: activeDocType ref (DocType), showGenerateDialog ref. OnMounted: Call documentStore.fetchDocuments(projectId), projectStore.fetchProjects (if not loaded). Computed: currentDocument (filter by activeDocType), project (find by projectId from projectStore), topic (project.topic), canGenerate (content empty or user confirmed). Template structure: Header section with project title, domain tag, status tag, back button. Main section: el-card for document tabs (DocumentTabs component), el-card for tech stack (TechStackPanel), el-card for generate buttons (Generate All, Generate PRD, Generate Frontend, Generate Backend). Use DocumentTabs with slot for MarkdownEditor. Add loading overlay when generating. Add error message display. Style: responsive layout with proper spacing. DO NOT use inline styles - use scoped CSS. DO NOT fetch documents without projectId validation.</action>
  <verify>
    <automated>grep -E "DocumentTabs|MarkdownEditor|TechStackPanel|fetchDocuments|generateDocument|projectId" frontend/src/views/ProjectDetail.vue | wc -l | grep -q "8"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/src/views/ProjectDetail.vue
    - DocumentTabs component imported and used
    - MarkdownEditor component imported and used
    - TechStackPanel component imported and used
    - documentStore.fetchDocuments called in onMounted
    - projectId extracted from route.params
    - Generate buttons present
    - Header with project info present
  </acceptance_criteria>
  <done>ProjectDetail page created with complete layout integrating all components.</done>
</task>

<task type="auto">
  <name>Task 2: Implement document generation handlers</name>
  <files>frontend/src/views/ProjectDetail.vue</files>
  <read_first>
    - frontend/src/views/ProjectDetail.vue (current state from Task 1)
    - frontend/src/stores/document.store.ts (generateDocument action)
    - frontend/src/api/ai.api.ts
  </read_first>
  <action>Add document generation handlers to ProjectDetail.vue. Create handleGenerateAll function: Calls generateDocument for each docType (PRD, FRONTEND, BACKEND) sequentially with proper loading states. Create handleGenerateSingle(docType) function: Calls documentStore.generateDocument(projectId, docType), shows loading during generation, handles success/error with ElMessage. Add confirm dialog before regeneration if document has content (warn user about overwrite per D-12). Handle 30s timeout gracefully (show timeout message). Show "生成中..." overlay during AI call. Update button disabled states during generation. DO NOT allow concurrent generation calls - queue them. DO NOT skip confirmation dialog for existing content.</action>
  <verify>
    <automated>grep -E "handleGenerateAll|handleGenerateSingle|generateDocument|ElMessage|ElMessageBox" frontend/src/views/ProjectDetail.vue | wc -l | grep -q "6"</automated>
  </verify>
  <acceptance_criteria>
    - handleGenerateAll function exists
    - handleGenerateSingle function exists
    - Confirmation dialog before regeneration
    - Loading state shown during generation
    - ElMessage for success/error feedback
    - Sequential generation (not concurrent)
  </acceptance_criteria>
  <done>Document generation handlers implemented with proper UX flow.</done>
</task>

<task type="auto">
  <name>Task 3: Implement document save and tech stack edit</name>
  <files>frontend/src/views/ProjectDetail.vue</files>
  <read_first>
    - frontend/src/views/ProjectDetail.vue (current state)
    - frontend/src/stores/document.store.ts (updateDocument action)
    - frontend/src/components/MarkdownEditor.vue (save emit)
    - frontend/src/components/TechStackPanel.vue (update emit)
    - frontend/src/api/project.api.ts (updateTechStack function to add)
  </read_first>
  <action>Add save handler for MarkdownEditor emit. Create handleDocumentSave function: Receives documentId and content from editor emit, calls documentStore.updateDocument(id, content). Show "保存中..." indicator briefly. Add success indicator after save completes. Handle save errors gracefully. Add tech stack update handler. Create handleTechStackUpdate function: Receives updated techStack string from TechStackPanel emit. Call new updateProjectTechStackApi(projectId, techStack) which calls PUT /api/projects/:id/techStack endpoint (added in Plan 02 Wave 1). Show success message after update. The tech stack displayed: if project.techStack exists (user modified), show that; otherwise show project.topic.techStack (original recommendation). Add updateProjectTechStackApi function to frontend/src/api/project.api.ts if not exists. Add manual save button in toolbar for explicit save. DO NOT call updateDocument on every keystroke - rely on debounce from editor. DO NOT block UI during save.</action>
  <verify>
    <automated>grep -E "handleDocumentSave|handleTechStackUpdate|updateDocument|updateProjectTechStack|@save|@update:techStack" frontend/src/views/ProjectDetail.vue frontend/src/api/project.api.ts | wc -l | grep -q "6"</automated>
  </verify>
  <acceptance_criteria>
    - handleDocumentSave function exists
    - handleTechStackUpdate function exists
    - Save handler connected to editor @save emit
    - TechStack handler connected to panel emit
    - updateProjectTechStackApi function exists in project.api.ts
    - PUT /api/projects/:id/techStack endpoint called
    - Save indicator shown briefly
    - Error handling for save failures
  </acceptance_criteria>
  <done>Document save and tech stack edit handlers implemented with explicit backend endpoint call.</done>
</task>

<task type="auto">
  <name>Task 4: Update router and Dashboard navigation</name>
  <files>frontend/src/router/index.ts, frontend/src/views/Dashboard.vue</files>
  <read_first>
    - frontend/src/router/index.ts (existing routes)
    - frontend/src/views/Dashboard.vue (handleProjectClick placeholder)
    - frontend/src/views/ProjectDetail.vue (page to route to)
  </read_first>
  <action>Add route for ProjectDetail in router/index.ts. Route: { path: '/projects/:id', name: 'ProjectDetail', component: () => import('@/views/ProjectDetail.vue'), meta: { requiresAuth: true } }. Update Dashboard.vue handleProjectClick function: Replace ElMessage.info placeholder with router.push(`/projects/${project.id}`). Add proper navigation without blocking. Ensure route guard works for new route (meta.requiresAuth: true). DO NOT add requiresAdmin - only requiresAuth for students. DO NOT change existing route order.</action>
  <verify>
    <automated>grep -E "/projects/:id|ProjectDetail|router.push.*projects" frontend/src/router/index.ts frontend/src/views/Dashboard.vue | wc -l | grep -q "4"</automated>
  </verify>
  <acceptance_criteria>
    - Route /projects/:id added to router
    - Route has meta: { requiresAuth: true }
    - ProjectDetail component imported
    - Dashboard handleProjectClick uses router.push
    - Placeholder ElMessage removed from Dashboard
  </acceptance_criteria>
  <done>Router updated and Dashboard navigation connects to ProjectDetail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route params -> Page | projectId from URL is validated via route |
| Page -> Store -> API | All authenticated via authMiddleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | Invalid projectId in URL | mitigate | Route validation + backend IDOR check handles this |
| T-03-05-02 | Denial of Service | Concurrent generation spam | mitigate | Sequential generation queue; button disabled during operation |
</threat_model>

<verification>
After completing all tasks:
1. Run `cd frontend && npm run build` — Vue must compile
2. Verify route exists in router
3. Verify Dashboard navigates correctly
4. Verify all handlers connected
5. Manual: Navigate from dashboard to project detail
</verification>

<success_criteria>
- [ ] ProjectDetail.vue page created with all components integrated
- [ ] Generate handlers with confirmation and loading states
- [ ] Save handler connected to editor
- [ ] Tech stack edit handler connected
- [ ] Route /projects/:id added
- [ ] Dashboard handleProjectClick navigates to project detail
- [ ] Vue build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-文档生成与AI服务/03-05-SUMMARY.md`
</output>