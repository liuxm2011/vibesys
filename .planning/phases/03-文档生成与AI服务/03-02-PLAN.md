---
phase: 03-文档生成与AI服务
plan: 02
type: execute
wave: 1
depends_on: [03-00]
files_modified:
  - backend/src/routes/documents.routes.ts
  - backend/src/routes/projects.routes.ts
  - backend/src/index.ts
autonomous: true
requirements:
  - DOC-05
  - DOC-06
  - DOC-07
user_setup: []

must_haves:
  truths:
    - "User can fetch all documents for their project"
    - "User can update document content (real-time save D-12)"
    - "User can view tech stack from topic"
    - "Project ownership verified before any document operation"
  artifacts:
    - path: "backend/src/routes/documents.routes.ts"
      provides: "Document CRUD REST endpoints"
      exports: ["GET /api/documents/:projectId", "PUT /api/documents/:id", "POST /api/documents"]
      contains: "authMiddleware"
    - path: "backend/src/routes/projects.routes.ts"
      provides: "Project tech stack modification endpoint"
      exports: ["PUT /api/projects/:id/techStack"]
      contains: "prisma.project.update"
    - path: "backend/src/index.ts"
      provides: "Route registration"
      contains: "app.use('/api/documents'"
  key_links:
    - from: "documents.routes.ts"
      to: "Document model"
      via: "prisma.document"
      pattern: "prisma\\.document\\.(find|create|update)"
    - from: "Document"
      to: "Project"
      via: "projectId FK"
      pattern: "project\\.userId"
    - from: "projects.routes.ts"
      to: "Project.techStack"
      via: "prisma.project.update"
      pattern: "data:.*techStack"
    - from: "Project.techStack (editable)"
      to: "Topic.techStack (source)"
      via: "Initial value inheritance"
      pattern: "project.techStack || project.topic.techStack"
---

<objective>
Wave 1 Backend Documents: Create document CRUD routes for fetching, creating, and updating documents. Implement real-time save functionality (D-12) with proper IDOR prevention. Support tech stack viewing via project->topic relation (DOC-06).

Purpose: Provide REST API for document operations with security and proper data flow.
Output: documents.routes.ts with GET, PUT, POST endpoints.
</objective>

<execution_context>
@/Users/liuxiangmiao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liuxiangmiao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-文档生成与AI服务/03-CONTEXT.md
@.planning/phases/03-文档生成与AI服务/03-RESEARCH.md
@.planning/phases/03-文档生成与AI服务/03-VALIDATION.md
</context>

<interfaces>
<!-- Key interfaces executor needs -->
From backend/prisma/schema.prisma (Wave 0 output):
```prisma
enum DocType {
  PRD
  FRONTEND
  BACKEND
}

model Document {
  id        Int       @id @default(autoincrement())
  projectId Int
  docType   DocType
  content   String    @db.Text
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  project   Project   @relation(fields: [projectId], references: [id], onDelete: Cascade)
  @@unique([projectId, docType])
  @@index([projectId])
}

model Project {
  // existing fields
  documents  Document[]
}
```

From backend/src/routes/projects.routes.ts (existing pattern):
```typescript
import { Router, Request, Response } from 'express';
import { authMiddleware } from '../middleware/auth.middleware.js';
import { prisma } from '../index.js';

const router = Router();
router.get('/', authMiddleware, async (req: Request, res: Response) => {
  const userId = req.user!.userId;
  // IDOR pattern: project.userId check
  const project = await prisma.project.findFirst({
    where: { id: projectId, userId }
  });
});
```

From backend/src/middleware/auth.middleware.ts (existing):
```typescript
// req.user!.userId available after authMiddleware
// req.user!.role available for role checks
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create GET documents endpoint</name>
  <files>backend/src/routes/documents.routes.ts</files>
  <read_first>
    - backend/prisma/schema.prisma (Document model, Project relation)
    - backend/src/routes/projects.routes.ts (route pattern, IDOR check pattern)
    - backend/src/middleware/auth.middleware.ts
  </read_first>
  <action>Create documents.routes.ts file. Add GET /api/documents/:projectId endpoint. Apply authMiddleware. Validate projectId is valid integer. Query prisma.project.findFirst with where: { id: projectId, userId: req.user!.userId } for IDOR prevention. If project not found, return 404. Query prisma.document.findMany with where: { projectId }. Include topic.techStack in response for DOC-06. Return { documents, techStack: project.topic.techStack }. If no documents exist, return empty array (lazy creation per RESEARCH.md Open Question 2). DO NOT return documents for projects not owned by user.</action>
  <verify>
    <automated>grep -E "router.get.*:projectId|authMiddleware|findFirst.*userId|findMany.*projectId" backend/src/routes/documents.routes.ts | wc -l | grep -q "4"</automated>
  </verify>
  <acceptance_criteria>
    - GET route defined with /:projectId parameter
    - authMiddleware applied before handler
    - Project ownership verified (userId check)
    - Documents fetched with prisma.document.findMany
    - techStack included in response from project.topic
    - 404 returned if project not found or not owned
  </acceptance_criteria>
  <done>GET documents endpoint created with IDOR prevention and techStack inclusion.</done>
</task>

<task type="auto">
  <name>Task 2: Create PUT document endpoint (real-time save)</name>
  <files>backend/src/routes/documents.routes.ts</files>
  <read_first>
    - backend/prisma/schema.prisma (Document model with updatedAt)
    - backend/src/routes/projects.routes.ts (route pattern)
    - backend/src/routes/documents.routes.ts (current state from Task 1)
  </read_first>
  <action>Add PUT /api/documents/:id endpoint for real-time save (D-12, DOC-05). Validate documentId from params. Validate content in body (non-empty, max 100KB per RESEARCH.md Pitfall 2). Query document with prisma.document.findFirst where: { id }, include: { project: true }. Verify document.project.userId === req.user!.userId for IDOR prevention. If not found or not owned, return 404. Update document with prisma.document.update where: { id }, data: { content }. Return { document: updated }. DO NOT allow updating documents of other users' projects. DO NOT accept content larger than 100KB.</action>
  <verify>
    <automated>grep -E "router.put.*:id|content|document\\.project\\.userId|prisma\\.document\\.update" backend/src/routes/documents.routes.ts | wc -l | grep -q "4"</automated>
  </verify>
  <acceptance_criteria>
    - PUT route defined with /:id parameter
    - Content validated (exists, size limit)
    - Document ownership verified via project.userId
    - Document updated with prisma.document.update
    - Updated document returned in response
    - 404 for unauthorized or missing document
  </acceptance_criteria>
  <done>PUT document endpoint created for real-time save with IDOR prevention.</done>
</task>

<task type="auto">
  <name>Task 3: Create POST document endpoint and register routes</name>
  <files>backend/src/routes/documents.routes.ts, backend/src/index.ts</files>
  <read_first>
    - backend/prisma/schema.prisma (DocType enum, Document model)
    - backend/src/routes/documents.routes.ts (current state)
    - backend/src/index.ts (route registration pattern)
  </read_first>
  <action>Add POST /api/documents endpoint for creating empty document (lazy creation). Body: { projectId, docType }. Validate projectId and docType (must be valid DocType enum value). Verify project ownership with userId check. Create document with prisma.document.create data: { projectId, docType, content: '' }. Handle @@unique constraint violation (if document already exists) by returning existing document instead of error. Return { document: createdOrExisting }. Add app.use('/api/documents', documentsRoutes) to index.ts. Export router from documents.routes.ts.</action>
  <verify>
    <automated>grep -E "router.post|DocType|prisma\\.document\\.create|app.use.*documents" backend/src/routes/documents.routes.ts backend/src/index.ts | wc -l | grep -q "4"</automated>
  </verify>
  <acceptance_criteria>
    - POST route defined for creating documents
    - docType validated against enum values
    - Project ownership verified
    - Document created with empty content
    - Unique constraint violation handled gracefully
    - Route registered in index.ts
    - Router exported from file
  </acceptance_criteria>
  <done>POST document endpoint created and routes registered in index.ts.</done>
</task>

<task type="auto">
  <name>Task 4: Create PUT tech stack endpoint (DOC-07)</name>
  <files>backend/src/routes/projects.routes.ts</files>
  <read_first>
    - backend/prisma/schema.prisma (Project.techStack field from Wave 0)
    - backend/src/routes/projects.routes.ts (current state)
  </read_first>
  <action>Add PUT /api/projects/:id/techStack endpoint to existing projects.routes.ts. Validate projectId is valid integer. Validate techStack in body (string, non-empty, reasonable length). Verify project ownership with prisma.project.findFirst where: { id: projectId, userId: req.user!.userId }. If not found or not owned, return 404. Update project with prisma.project.update where: { id }, data: { techStack }. Return { project: { id, techStack } }. DO NOT allow modifying tech stack of other users' projects. This endpoint enables DOC-07: 学生可修改技术栈选择.</action>
  <verify>
    <automated>grep -E "router.put.*techStack|techStack.*body|project\\.userId|prisma\\.project\\.update.*techStack" backend/src/routes/projects.routes.ts | wc -l | grep -q "4"</automated>
  </verify>
  <acceptance_criteria>
    - PUT route defined with /:id/techStack path
    - techStack validated in request body
    - Project ownership verified (userId check)
    - Project.techStack updated with prisma.project.update
    - Updated techStack returned in response
    - 404 for unauthorized or missing project
  </acceptance_criteria>
  <done>PUT tech stack endpoint created for DOC-07 modification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Untrusted input: projectId, documentId, content |
| API -> Database | Trusted operations after auth + ownership verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | IDOR - access other user's documents | mitigate | authMiddleware + project.userId check in every endpoint |
| T-03-02-02 | Tampering | XSS in Markdown content | accept | CodeMirror handles raw Markdown; no HTML rendering in backend |
| T-03-02-03 | Denial of Service | Large content upload | mitigate | 100KB limit on content field; reject oversized payloads |
| T-03-02-04 | Tampering | Invalid docType enum | mitigate | Validate docType against DocType enum values before create |
</threat_model>

<verification>
After completing all tasks:
1. Run `cd backend && npm run build` — TypeScript must compile
2. Verify GET endpoint has IDOR check (project.userId)
3. Verify PUT endpoint has ownership check
4. Verify POST handles unique constraint violation
5. Verify route registered in index.ts
</verification>

<success_criteria>
- [ ] GET /api/documents/:projectId returns documents + techStack
- [ ] GET endpoint has IDOR prevention (userId check)
- [ ] PUT /api/documents/:id updates content with real-time save
- [ ] PUT endpoint has ownership verification
- [ ] POST /api/documents creates empty document
- [ ] POST handles existing document gracefully
- [ ] PUT /api/projects/:id/techStack modifies tech stack (DOC-07)
- [ ] PUT techStack endpoint has ownership verification
- [ ] Routes registered in index.ts
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-文档生成与AI服务/03-02-SUMMARY.md`
</output>