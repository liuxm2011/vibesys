---
phase: 03-文档生成与AI服务
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/tests/document.test.ts
  - backend/tests/ai.test.ts
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
user_setup: []

must_haves:
  truths:
    - "Document table exists with projectId, docType, content fields"
    - "DocType enum has PRD, FRONTEND, BACKEND values"
    - "Test files exist for document and AI modules"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Document model + DocType enum"
      contains: "enum DocType"
      contains: "model Document"
    - path: "backend/tests/document.test.ts"
      provides: "Test skeleton for document CRUD"
      min_lines: 10
    - path: "backend/tests/ai.test.ts"
      provides: "Test skeleton for AI generation"
      min_lines: 10
  key_links:
    - from: "model Document"
      to: "model Project"
      via: "projectId foreign key"
      pattern: "@relation.*Project"
---

<objective>
Wave 0 foundation: Extend Prisma schema with Document model and DocType enum per D-10~13. Create test file skeletons for document and AI modules to support TDD validation.

Purpose: Establish database schema before backend routes; provide test scaffolds for Wave 1+ validation.
Output: Schema migration, test skeleton files.
</objective>

<execution_context>
@/Users/liuxiangmiao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liuxiangmiao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-文档生成与AI服务/03-CONTEXT.md
@.planning/phases/03-文档生成与AI服务/03-RESEARCH.md
@.planning/phases/03-文档生成与AI服务/03-VALIDATION.md
</context>

<interfaces>
<!-- Key interfaces from existing schema that executor needs -->
From backend/prisma/schema.prisma (existing):
```prisma
model Project {
  id              Int           @id @default(autoincrement())
  userId          Int
  topicId         Int
  status          ProjectStatus @default(NOT_STARTED)
  documentsRef    Json?         // Phase 3 placeholder - will be deprecated
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
  user            User          @relation(fields: [userId], references: [id])
  topic           Topic         @relation(fields: [topicId], references: [id])
  @@index([userId])
  @@index([status])
  @@index([topicId])
}
```

From backend/prisma/schema.prisma (existing Domain enum):
```prisma
enum Domain {
  SE  // 软件工程
  BD  // 大数据
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend Prisma schema with Document model and Project techStack</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    - backend/prisma/schema.prisma (current state, Project model, Domain enum)
  </read_first>
  <action>Add DocType enum with PRD, FRONTEND, BACKEND values (D-13). Add Document model with fields: id (autoincrement), projectId (foreign key to Project), docType (DocType enum), content (String @db.Text for large Markdown), createdAt, updatedAt (D-13). Add relation: Document.project -> Project, Project.documents -> Document[] (D-10). Add @@unique([projectId, docType]) constraint for one document per type per project (D-11). Add @@index([projectId]) for query optimization. Add techStack field to Project model: techStack String? (nullable, stores user's modified tech stack preference - DOC-07). Initial techStack from Topic.techStack, user can modify via PUT endpoint. Update Project.documentsRef comment to mark as deprecated placeholder. DO NOT remove documentsRef field to maintain backward compatibility with Phase 2 projects.</action>
  <verify>
    <automated>grep -E "enum DocType|model Document|projectId|docType|content|techStack" backend/prisma/schema.prisma | wc -l | grep -q "7"</automated>
  </verify>
  <acceptance_criteria>
    - File contains "enum DocType { PRD FRONTEND BACKEND }"
    - File contains "model Document"
    - Document model has "projectId Int" field
    - Document model has "docType DocType" field  
    - Document model has "content String @db.Text" field
    - Document model has "@@unique([projectId, docType])"
    - Project model has "documents Document[]" relation
    - Project model has "techStack String?" field (nullable for optional modification)
  </acceptance_criteria>
  <done>Prisma schema extended with DocType enum, Document model, and Project.techStack field. Schema validates with prisma validate.</done>
</task>

<task type="auto">
  <name>Task 2: Create document test skeleton</name>
  <files>backend/tests/document.test.ts</files>
  <read_first>
    - backend/prisma/schema.prisma (Document model structure)
    - backend/src/routes/projects.routes.ts (route pattern reference)
  </read_first>
  <action>Create Vitest test file skeleton for document CRUD operations. Include describe blocks for: GET /api/documents/:projectId (fetch all documents for project), PUT /api/documents/:id (update content - D-12 real-time save), POST /api/documents (create empty document). Each test block should have placeholder test('should ...') with TODO markers. Import prisma mock pattern from existing test setup. Use existing vitest.config.ts conventions.</action>
  <verify>
    <automated>grep -c "describe\|test\|TODO" backend/tests/document.test.ts | grep -q "5"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at backend/tests/document.test.ts
    - File contains "import { describe, test, expect } from 'vitest'"
    - File contains describe block for document routes
    - File has at least 3 placeholder test cases
    - File references DOC-01~03, DOC-05 requirements
  </acceptance_criteria>
  <done>Test skeleton file created with placeholder tests for document CRUD operations.</done>
</task>

<task type="auto">
  <name>Task 3: Create AI test skeleton</name>
  <files>backend/tests/ai.test.ts</files>
  <read_first>
    - backend/prisma/schema.prisma (DocType enum, Document model)
    - backend/src/routes/projects.routes.ts (route pattern reference)
  </read_first>
  <action>Create Vitest test file skeleton for AI generation operations. Include describe blocks for: POST /api/ai/generate (DOC-04 AI content generation), domain differentiation (SE vs BD templates - D-08/09), MiniMax API integration (mocked). Each test block should have placeholder test('should ...') with TODO markers. Include mock setup for OpenAI SDK with custom baseURL pattern.</action>
  <verify>
    <automated>grep -c "describe\|test\|TODO" backend/tests/ai.test.ts | grep -q "4"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at backend/tests/ai.test.ts
    - File contains "import { describe, test, expect } from 'vitest'"
    - File contains describe block for AI generation
    - File has at least 3 placeholder test cases
    - File references DOC-04 requirement and D-08/09 domain differentiation
  </acceptance_criteria>
  <done>Test skeleton file created with placeholder tests for AI generation operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database schema | No external input crosses here in Wave 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00-01 | N/A | Schema extension | accept | Wave 0 only creates schema; no runtime exposure |
</threat_model>

<verification>
After completing all tasks:
1. Run `cd backend && npx prisma validate` — schema must validate
2. Verify test skeleton files exist with correct structure
3. Confirm Document model has unique constraint [projectId, docType]
</verification>

<success_criteria>
- [ ] DocType enum exists with PRD, FRONTEND, BACKEND
- [ ] Document model exists with projectId FK, docType, content TEXT
- [ ] Document has @@unique([projectId, docType]) constraint
- [ ] Project.documents relation added
- [ ] Project.techStack String? field added (DOC-07)
- [ ] Test skeleton files created
- [ ] Prisma schema validates successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-文档生成与AI服务/03-00-SUMMARY.md`
</output>