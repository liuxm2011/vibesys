---
phase: 03-文档生成与AI服务
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - frontend/src/components/MarkdownEditor.vue
  - frontend/src/components/TechStackPanel.vue
  - frontend/src/components/DocumentTabs.vue
autonomous: true
requirements:
  - DOC-05
  - DOC-06
  - DOC-07
user_setup: []

must_haves:
  truths:
    - "Markdown editor component renders with syntax highlighting"
    - "Editor has auto-save with 500ms debounce (D-12)"
    - "Tech stack panel displays and allows editing"
    - "Document tabs switch between PRD/Frontend/Backend"
  artifacts:
    - path: "frontend/src/components/MarkdownEditor.vue"
      provides: "CodeMirror-based Markdown editor"
      contains: "vue-codemirror"
      contains: "@change"
    - path: "frontend/src/components/TechStackPanel.vue"
      provides: "Tech stack display and edit UI"
      contains: "el-tag"
      contains: "el-select"
    - path: "frontend/src/components/DocumentTabs.vue"
      provides: "Tab navigation for document types"
      contains: "el-tabs"
      contains: "DocType"
  key_links:
    - from: "MarkdownEditor.vue"
      to: "document.store.ts"
      via: "props + emit pattern"
    - from: "TechStackPanel.vue"
      to: "document.store.ts"
      via: "techStack state"
---

<objective>
Wave 3 Frontend UI Components: Create reusable Vue components for Markdown editing (vue-codemirror), tech stack display/edit, and document tab navigation. Implement auto-save debounce (D-12) and proper component architecture.

Purpose: Build reusable UI components that will compose the ProjectDetail page.
Output: MarkdownEditor.vue, TechStackPanel.vue, DocumentTabs.vue components.
</objective>

<execution_context>
@/Users/liuxiangmiao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liuxiangmiao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-文档生成与AI服务/03-CONTEXT.md
@.planning/phases/03-文档生成与AI服务/03-RESEARCH.md
</context>

<interfaces>
<!-- Key interfaces executor needs -->
From frontend/src/stores/document.store.ts (Wave 2):
```typescript
export const useDocumentStore = defineStore('document', () => {
  const documents = ref<Document[]>([]);
  const techStack = ref<string[]>([]);
  const generating = ref<boolean>(false);
  const loading = ref<boolean>(false);
  // ... actions: fetchDocuments, updateDocument, generateDocument
});
```

From frontend/src/types/document.ts (Wave 2):
```typescript
export type DocType = 'PRD' | 'FRONTEND' | 'BACKEND';
export interface Document {
  id: number;
  projectId: number;
  docType: DocType;
  content: string;
  createdAt: string;
  updatedAt: string;
}
```

From frontend/src/views/Dashboard.vue (existing Element Plus pattern):
```vue
<script setup lang="ts">
import { ElMessage, ElMessageBox } from 'element-plus';
</script>

<template>
  <el-card>
    <el-button type="primary">Action</el-button>
  </el-card>
</template>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install vue-codemirror dependencies</name>
  <files>frontend/package.json</files>
  <read_first>
    - frontend/package.json (current dependencies)
  </read_first>
  <action>Install vue-codemirror and related packages. Run npm install vue-codemirror @codemirror/lang-markdown @codemirror/theme-one-dark in frontend directory. This provides the Markdown editor component with syntax highlighting and dark theme support per RESEARCH.md Standard Stack section. DO NOT install CodeMirror 5 packages - use CodeMirror 6 via vue-codemirror.</action>
  <verify>
    <automated>grep -E "vue-codemirror|@codemirror/lang-markdown|@codemirror/theme-one-dark" frontend/package.json | wc -l | grep -q "3"</automated>
  </verify>
  <acceptance_criteria>
    - vue-codemirror in dependencies
    - @codemirror/lang-markdown in dependencies
    - @codemirror/theme-one-dark in dependencies
    - package.json updated with new dependencies
  </acceptance_criteria>
  <done>vue-codemirror and CodeMirror markdown packages installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create MarkdownEditor component</name>
  <files>frontend/src/components/MarkdownEditor.vue</files>
  <read_first>
    - frontend/src/views/Dashboard.vue (Vue component pattern)
    - frontend/src/stores/document.store.ts (updateDocument action)
    - frontend/src/types/document.ts (Document type)
    - frontend/package.json (vue-codemirror installed)
  </read_first>
  <action>Create MarkdownEditor.vue component with vue-codemirror integration. Props: content (string, required), documentId (number, required), disabled (boolean, default false). Setup: Import Codemirror from 'vue-codemirror', markdown from '@codemirror/lang-markdown', oneDark from '@codemirror/theme-one-dark', EditorView from '@codemirror/view'. Create extensions array: [markdown(), oneDark, EditorView.theme({ '&': { height: '400px' }, '.cm-scroller': { overflow: 'auto' } })]. Create localContent ref initialized from props.content. Implement debounce helper (useDebounceFn from @vueuse/core or custom 500ms debounce). Emit 'save' event on debounced change. Emit 'change' event on immediate change. Template: codemirror with v-model="localContent", extensions, placeholder, disabled prop. Style: scoped CSS for editor container. DO NOT call API directly in component - emit events to parent. DO NOT use immediate save - debounce required (D-12).</action>
  <verify>
    <automated>grep -E "vue-codemirror|@codemirror/lang-markdown|debounce|emit.*save|v-model" frontend/src/components/MarkdownEditor.vue | wc -l | grep -q "6"</automated>
  </verify>
  <acceptance_criteria>
    - Codemirror component imported from vue-codemirror
    - markdown() extension from @codemirror/lang-markdown
    - oneDark theme imported
    - Props defined: content, documentId, disabled
    - 500ms debounce implemented
    - 'save' and 'change' events emitted
    - Editor height set via EditorView.theme
  </acceptance_criteria>
  <done>MarkdownEditor component created with CodeMirror integration and auto-save debounce.</done>
</task>

<task type="auto">
  <name>Task 3: Create TechStackPanel component</name>
  <files>frontend/src/components/TechStackPanel.vue</files>
  <read_first>
    - frontend/src/views/Dashboard.vue (Element Plus pattern)
    - frontend/src/stores/document.store.ts (techStack state)
    - frontend/src/types/document.ts
  </read_first>
  <action>Create TechStackPanel.vue for displaying and editing tech stack (DOC-06, DOC-07). Props: techStack (string[], required), editable (boolean, default true). Emit: 'update:techStack' when modified. Template: el-card with header "技术栈方案". Content: el-tag display for each tech item, closable if editable. el-select for adding new tech options (placeholder: '添加技术栈'). Options: Vue, React, Node.js, Express, MySQL, MongoDB, Redis, Prisma, Tailwind, Element Plus, etc. Show loading state if techStack is empty or being loaded. Style: flex layout for tags. DO NOT hardcode all possible tech stacks - provide common options. DO NOT allow empty tech stack additions.</action>
  <verify>
    <automated>grep -E "el-card|el-tag|el-select|techStack|emit.*update" frontend/src/components/TechStackPanel.vue | wc -l | grep -q "6"</automated>
  </verify>
  <acceptance_criteria>
    - el-card component used
    - el-tag for each tech item
    - el-select for adding new tech
    - Props: techStack, editable
    - Emit: update:techStack
    - Common tech options provided
  </acceptance_criteria>
  <done>TechStackPanel component created for tech stack display and editing.</done>
</task>

<task type="auto">
  <name>Task 4: Create DocumentTabs component</name>
  <files>frontend/src/components/DocumentTabs.vue</files>
  <read_first>
    - frontend/src/views/Dashboard.vue (Element Plus tabs pattern if exists, else el-tabs docs)
    - frontend/src/types/document.ts (DocType)
    - frontend/src/stores/document.store.ts
  </read_first>
  <action>Create DocumentTabs.vue for switching between document types. Props: documents (Document[], required), activeDocType (DocType, default 'PRD'). Emit: 'update:activeDocType' when tab changed. Template: el-tabs with v-model="activeTab". Tab items: PRD (label: 'PRD文档'), FRONTEND (label: '前端文档'), BACKEND (label: '后端文档'). Each tab contains slot for content. Show "未生成" indicator if document has empty content. Show "生成中..." indicator if generating state for that docType. Computed: activeTab maps to activeDocType prop. Method: handleTabChange emits update. Style: tab styling via Element Plus. DO NOT render document content directly - use slot pattern for parent to inject MarkdownEditor.</action>
  <verify>
    <automated>grep -E "el-tabs|DocType|PRD|FRONTEND|BACKEND|emit|slot" frontend/src/components/DocumentTabs.vue | wc -l | grep -q "8"</automated>
  </verify>
  <acceptance_criteria>
    - el-tabs component used
    - Three tabs: PRD, FRONTEND, BACKEND
    - Props: documents, activeDocType
    - Emit: update:activeDocType on tab change
    - Slot pattern for content injection
    - Empty/generating indicators present
    - DocType labels in Chinese
  </acceptance_criteria>
  <done>DocumentTabs component created for document type navigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend components | No direct API calls; events to parent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | XSS in Markdown content display | accept | CodeMirror renders raw Markdown; no HTML interpretation; backend sanitization if rendered |
</threat_model>

<verification>
After completing all tasks:
1. Run `cd frontend && npm run build` — TypeScript/Vue must compile
2. Verify vue-codemirror packages in package.json
3. Verify MarkdownEditor has debounce
4. Verify TechStackPanel has edit capability
5. Verify DocumentTabs has slot pattern
</verification>

<success_criteria>
- [ ] vue-codemirror packages installed
- [ ] MarkdownEditor component with CodeMirror + debounce
- [ ] TechStackPanel component with display + edit
- [ ] DocumentTabs component with slot pattern
- [ ] All components use TypeScript + script setup
- [ ] Vue build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-文档生成与AI服务/03-04-SUMMARY.md`
</output>